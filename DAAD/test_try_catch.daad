# اختبار try/catch/throw

دالة: الرئيسية() -> فراغ :-
    حاول :-
        متغير: س: رقم = 10
        إذا(س > 5) :-
            أطلق("القيمة كبيرة جداً")
        نهاية
    امسك(الخطأ) :-
        اطبع("تم التقاط الخطأ:")
        اطبع(الخطأ)
    نهاية
    توقف
نهاية
