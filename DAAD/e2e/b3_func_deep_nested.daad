دالة: noop(x: رقم) -> رقم :-
    ارجع(x)
نهاية

دالة: identity(x: رقم) -> رقم :-
    ارجع(noop(x))
نهاية

دالة: double(x: رقم) -> رقم :-
    ارجع(identity(x) + identity(x))
نهاية

دالة: الرئيسية() -> رقم :-
    ارجع(double(5))
نهاية
