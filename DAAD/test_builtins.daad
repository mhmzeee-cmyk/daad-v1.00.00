# اختبار الـ builtins

دالة: الرئيسية() -> فراغ :-
    # اختبار الطباعة
    اطبع("مرحباً!")
    اطبع("العدد: 42")

    # اختبار الذاكرة
    متغير: مؤشر: رقم = احجز(100)
    حرر(مؤشر)
    متغير: مؤشر2: رقم = غيّر_حجم(مؤشر، 200)

    # اختبار الإيقاف
    توقف
نهاية
