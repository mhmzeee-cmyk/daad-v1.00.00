دالة: الرئيسية() -> رقم :-
    اطبع("النتيجة:", 42)
    متغير: عدد: رقم = 0
    ادخل(عدد)
    متغير: أ: رقم = 10
    متغير: ب: رقم = 20
    متغير: ج: رقم = أ + ب
    اطبع("المجموع:", ج)
    ارجع(ج)
نهاية
