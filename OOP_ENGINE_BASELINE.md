# خط الأساس: محرك OOP الحالي قبل أي توسيع (قراءة + تشغيل فقط، بلا كود جديد)

> **التاريخ:** 2026-09-12 — **الغرض:** توثيق الوضع الحالي الدقيق لبرمجة الكائنات في محرك ض (الذي تُنفَّذ عليه التحديات) قبل تنفيذ الخيار (أ).
> **المنهجية:** قراءة المصدر (ملف:سطر) + تشغيل 30+ برنامج استكشاف عبر `executeDhad`/`compileDhad` على السيرفر + مقارنة المرايا + فحص starters الدروس الفعلية.
> **القاعدة:** لم يُكتب أي كود إنتاجي؛ برامج الاختبار في `/tmp/opencode/oop-probe/` (`probe.js`, `mirror.js`, `results.json`)؛ لم تتغير DB (الاستدعاءات مباشرة بلا HTTP)؛ السيرفر الحي لم يُمس.
> `FACT` = مثبت بملف:سطر أو مخرج تنفيذ. `INFERENCE` = استنتاج مُعلَّم.

## 0. الخلاصة التنفيذية

- المحرك يملك **نموذج OOP هجين (C#/JS) حقيقيًا وعاملًا جزئيًا**: `صنف/فئة` + بواني **عارية مسماة باسم الصنف** + `يرث` + `عام/خاص/محمي` + `هيكل` + `هذا` + `جديد Type(args)` — كلها تُترجم وتعمل (مُثبت تنفيذيًا).
- **مولّد الكود كامل وجاهز**: دوال بانية + سلسلة prototype + استدعاء باني الأب + حقول خاصة عبر closures + static (`codegen.js:227-331`).
- **الفجوة دقيقة ومحددة، لا فراغًا كاملًا**: المحتوى (T4/T7) يُدرّس **لهجة مختلفة** (`دالة جديد(...)` + `جديد(...)` المصنعي + أسماء محجوزة) لا يقبلها المحلل. النتيجة: 51 تحديًا غير قابلة للحل كما هي مكتوبة، رغم أن 70% من البنى المطلوبة مدعومة.
- **المرايا متطابقة سلوكيًا**: Web == server في 7/7 حالات فحص (نفس الأحكام ونفس رسائل الأخطاء حرفيًا).
- **مفاجأة إيجابية**: الباني العاري المنمّط (`شكل(صحيح ن)`) يعمل نهاية-لنهاية (`جديد شكل(7)` → `7`)، والوراثة (`يرث`) تعمل تنفيذيًا (`ك.صوت()` → `صوت`).

## 1. خريطة المحرك (FACT)

| الملف (Web) | الدور | الأسطر | المرآة (server) | الفرق |
|---|---|---|---|---|
| `js/dhad-lexer.js` | توكنز + قاموس (~103 كلمة) | 707 | `utils/dhad/lexer.js` | 703 (4- أسطر) |
| `js/dhad-parser.js` | نزولي recursive + Pratt (32 عقدة) | 1599 | `utils/dhad/parser.js` | 1572 (27- سطرًا) |
| `js/dhad-ast.js` | عقد AST (~50) | 649 | `utils/dhad/ast.js` | 639 (10- أسطر) |
| `js/dhad-codegen.js` | مولد JS + runtime | 1082 | `utils/dhad/codegen.js` | 1065 (17- سطرًا) |
| `js/dhad.js` (264) + `dhadCompiler.js` | `compile()/run()` + بوابة السيرفر | — | `utils/dhadCompiler.js` + `dhadSandbox.js` (321) | — |

- `frontend-web/js/*` مطابقة لـ Web بايتًا (`diff` صفر سابقًا). فروقات server (~4-27 سطرًا) **لم تُفحص سطرًا سطرًا** — لكن التكافؤ السلوكي مثبت بـ mirror probe (§6).
- مسار التحديات يستخدم **نسخة السيرفر حصرًا** (`student.js:277-278` → `executeDhad` → `dhadCompiler.compile` → `vm`).

## 2. النموذج المصمم (من الكود — FACT مع ملف:سطر)

**الإعلان** (`parser.js:585-646`): `صنف Name [يرث Parent] { ... }` — ملاحظة: `فئة` مرادف محلليًا (`فئة`→`KW_CLASS`).
**الأعضاء**: معدّلات `عام/خاص/محمي` + `ساكن` (`:600-608`) + صيغة `الاسم : النوع` (`:615-631`) + أي عبارة/دالة.
**الباني**: فرع خاص وحيد (`:610-614`): **معرّف عارٍ يساوي اسم الصنف متبوعًا بـ `(`** → `parseConstructor` (`:649+`) الذي يشترط أزواج `نوع اسم` للمعاملات.
**الإنشاء**: `جديد Type(args)` تعبير بادئ (`:778-793` + `:1216-1227`) → `NewExprAST`.
**`هذا`**: تُترجم حرفيًا لـ `this` (`:1212-1213`).
**التوليد** (`codegen.js:227-331`): دالة بانية بمقادير الباني + `Parent.call/apply` + حقول خاصة closures + `defineProperty` + دوال على `this` (+static) + `X.prototype = Object.create(P.prototype)`.
**الكلمات المحجوزة المؤكدة** (تفريغ lexer حي): `صنف فئة واجهة عام خاص محمي مجرّد يرث هيكل هذا جديد` + `س ص` (محاور!) + `دائرة→KW_CIRCLE` + `مستطيل→KW_RECTANGLE` + مفاجأة: `مربع خط صورة نقطة شكل حيوان...` **ليست** كلمات (`isKeyword=false`) — أي أن `مستطيل` كاسم صنف يجب أن يعمل... لكنه فشل (`C15`)! ← انظر §5.3 (السبب: على الأرجح قاعدة تحليلية أخرى، قيد التحقيق المفتوح — **INFERENCE**: ربما تعارض `مستطيل(` مع استدعاء دالة رسم مدمجة في سياق التعبير، لا الـ lexer).

## 3. مصفوفة الاستكشاف الكاملة (FACT — مخرجات تنفيذية)

### 3.1 يعمل اليوم (compile OK + run صحيح)

| البرنامج | الدليل |
|---|---|
| صنف فارغ + طباعة | `تم` |
| دالة داخل صنف + استدعاء تُرجع 42 | `42` (الدوال والاستدعاء والإرجاع تعمل) |
| **باني عارٍ منمّط `شكل(صحيح ن)` + `جديد شكل(7)`** | **`7`** (الباني يُنفَّذ ويورّث الحقول!) |
| وراثة `يرث` + استدعاء موروث | `صوت` (prototype chain يعمل) |
| `عام/خاص/محمي` (تحليل فقط) | تُقبل (الإنفاذ الدلالي غير مُتحقق — UNKNOWN) |
| عضو `الاسم : نص` | يعمل (صيغة النقطتين تحتاج اسمًا طويلًا — `س` alone تفشل لأنها `KW_X`!) |
| `هيكل` فارغ | يعمل |
| `مجرّد صنف` | يُتحلل (يُتجاهل كمعدّل — سلوكيًا كصنف عادي) |
| `فئة` كمرادف | يعمل |
| `new` يُنشئ كائنًا حقيقيًا | `[object Object]` |
| `هذا.ن = ن` داخل دالة | يعمل |
| فاصلة عربية `،` | تعمل (`11`) |

### 3.2 مكسور اليوم (مع السبب الجذري)

| # | الصيغة (محتوى الدروس) | الفشل | السبب الجذري (FACT) |
|---|---|---|---|
| R1 | `دالة جديد(...)` داخل الصنف | تحليل: `متوقع IDENTIFIER حصل على "جديد"` | فرع الباني (`:611`) يشترط معرّفًا عاريًا؛ `دالة` كلمة → يسقط لمسار دالة عادية فيرفض اسم `جديد` (كلمة محجوزة) |
| R2 | `جديد(7)` / `جديد(5، 10)` (مصنعي بلا نوع) | `متوقع IDENTIFIER حصل على "("` | `parseNewExpr` تشترط اسم نوع (`:780`) — لا استدلال من LHS |
| R3 | `صنف دائرة` (و `مستطيل`؟ — §5.3) | `متوقع IDENTIFIER حصل على "دائرة"` | `دائرة→KW_CIRCLE` (تفريغ حي) — الكلمات الرسومية تخطف المعرفات |
| R4 | `واجهة قابل { دالة هجاء() }` | `متوقع LBRACE حصل على "}"` | لا توجد قاعدة `parseInterface` — الكلمة موجودة بلا نحو (يتوقف عند أول `}`) |
| R5 | باني `دالة شکل` (بنفس الاسم لكن بـ `دالة`) | **ينجح زائفًا**: يُبنى كدالة عادية → `new` يتجاهلها → `عدم` بدل القيمة! | تفريغ AST حي: `دالة شکل(` → `["FunctionDecl"]` مقابل العاري → `["ConstructorDecl"]` — **أخطر الأنواع: صمت + نتيجة خاطئة** |
| R6 | مثال المحرر (`فئة` + `دالة()` مجهولة + `جديد ح1 = ح()`) | `متوقع IDENTIFIER حصل على "("` | دوال مجهولة + صيغة `جديد x =` غير مدعومتين (المحرر يوثق لهجة ثالثة!) |

## 4. المحتوى مقابل المحرك (FACT)

- **17 starter دروس** لا تُترجم (10 في T4 كلها `دالة جديد` + `صنف دائرة`، و7 في T7: وراثة/أصل/وصول/محمي/مجرّد/واجهة/هيكل).
- **51 تحديًا تشترط `صنف/جديد`** في المتطلبات النصية (`must_use_keyword` — الفاحص نصي، فأي ظهور للكلمة يُرضيه، لكن الكود يجب أن يُترجم ويعمل).
- تمارين T4 النموذجية (`مستطيل 6×4 → 24`) **مستحيلة حرفيًا**: لا صياغة بانية مقبولة + لا إنشاء يعمل + الاسم `مستطيل` نفسه مرفوض (§5.3).
- إيجابي: تمارين يمكن حلها بالمجموعة المدعومة **إن أُعيدت صياغتها** (بواني عارية + `new` منمّط + أسماء غير محجوزة) — مثال حي مُثبت: `شكل(صحيح ن)` → `7`.

## 5. نقاط غامضة/مفتوحة (UNKNOWN بصراحة)

- **5.1**: `مجرّد/عام/خاص/ساكن` تُقبل نحويًا — هل لها أي أثر دلالي؟ (غير مُختبر تنفيذيًا بعمق؛ `خاص` لها emit مخصص يُرجح أثرًا).
- **5.2**: `يرث` مع حقول الأب و`super` — الوراثة الأساسية تعمل؛ `الأصل` (super) غير مُختبر.
- **5.3**: لماذا رُفض `مستطيل` رغم أنه ليس keyword؟ (INFERENCE: تعارض محللي آخر — يحتاج تفريغ توكنز للحالة الكاملة).
- **5.4**: سلوك متصفح التشغيل (DOM/Canvas) لم يُنفَّذ هنا — التكافؤ مثبت على مستوى الترجمة فقط (§6).

## 6. المرايا (FACT)

- Web مقابل server: **7/7 تطابقًا** في الأحكام ورسائل الأخطاء حرفيًا (`mirror.js`: فارغ/باني-عاري/دالة-جديد/وراثة/مصنعي/محجوز/واجهة).
- الفروقات السطرية (4/27/10/17) لا تمس مسارات OOP المفحوصة — INFERENCE: انحراف تجميلي/تعليقات.
- C++ (للمقارنة فقط): `صنف/فئة→class` و`جديد→new` (`Keywords.cpp:43-44,61`) + `parseClassDeclaration` (`Parser.cpp:1197`) + توليد (`CodeGen.cpp:520`) — خط C++ مكتمل **كمحوّل** (ينتج C++‎)، لا منفّذ تحديات.

## 7. متطلبات الخيار (أ) — تفكيك مبني على الأدلة (تقديرات INFERENCE)

| الحزمة | النطاق من الأدلة | الملفات | الحجم |
|---|---|---|---|
| P1: بانٍ بصيغة المحتوى | قبول `دالة جديد(...)` كباني (تحويله لـ ConstructorDecl) + توثيق الصيغة العارية القائمة | `parser.js (~:610)` + اختبارات | S |
| P2: إنشاء مرن | `جديد Type(args)` موجود؛ تقرير مصير `جديد(...)` المصنعي (رفض واضح برسالة عربية أفضل من دعمه؟) | `parser.js:778` + رسائل | S |
| P3: الكلمات السياقية **(الأصعب)** | `دائرة/مستطيل/...` كمعرفات بعد `صنف`/`جديد` رغم كونها كلمات رسومية؛ و`س/ص` كأعضاء | `lexer.js` (قاموس سياقي) + `parser.js` + اختبارات انحدار للرسوميات | L |
| P4: `واجهة/مجرّد` | قواعد إعلان (تجاهل دلالي موثق أو إنفاذ minimal) | `parser.js` + `ast.js` + `codegen.js` | M |
| P5: إصلاح الصمت الخطر | R5: `دالة` بنفس اسم الصنف يجب أن تُبنى كباني أو تُرفض بخطأ واضح (لا `عدم` صامتًا) | `parser.js:610` + `codegen.js:237` | S |
| P6: ترحيل المحتوى | إعادة كتابة 17 starter + مراجعة 51 متطلبات على اللهجة المدعومة (بعد تثبيت P1-P4) | `tests/challenges_tier{4,7}.js` + إعادة بذر + `sample.js` موسّع | M (تأليفي) |
| P7: توحيد التوثيق | مثال المحرر (`dhad-editor-page.js:1013`) يوثق لهجة ثالثة مكسورة — مزامنته مع اللهجة المدعومة | `dhad-editor-page.js` ×3 مرايا | S |
| P8: بوابة انحدار | مصفوفة `probe.js` + `mirror.js` كاختبارات دائمة + توسيع `cheatingDetection` لبرامج OOP | `tests/unit/` ×2 | S |

**ترتيب مقترح**: P5 (سلامة) → P1+P2 (نحو) → P4 → P3 (الأصعب، قد يُجزّأ: صنف-أسماء أولًا) → P6 (محتوى) → P7+P8.
**ما لا يحتاج عملًا**: مولد البواني/الوراثة/الخاص (`codegen.js:227-331` كامل)، `هذا`، `هيكل` الأساسي، المرايا (متطابقة).

## 8. الحكم الحالي (TL;DR)

- يعمل اليوم: أصناف فارغة/بدوال، بوانٍ عارية منمطة، `new` منمط، وراثة أساسية، `this`، struct أساسي، aliases.
- مكسور اليوم: صيغة المحتوى (`دالة جديد`، مصنعي `جديد()`)، أسماء رسومية كمعرفات، `واجهة`، مثال المحرر، وصمت R5.
- مسار T4/T7 كما هو مكتوب: **مغلق**. أقصر طريق لفتحه: P5+P1+P2 (أيام معدودة) ثم P6 محتوى؛ P3 للكمال.

## 9. إعادة الإنتاج

```bash
cd Studio/desktop-app/server
node /tmp/opencode/oop-probe/probe.js        # مصفوفة 22 (نتائج: results.json)
node /tmp/opencode/oop-probe/mirror.js       # تكافؤ Web/server (7/7)
# مسبار يدوي: node -e "const {executeDhad}=require('./src/utils/dhadSandbox'); ..."
```
