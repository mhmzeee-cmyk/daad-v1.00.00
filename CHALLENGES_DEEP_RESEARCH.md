# بحث مفصل: التحديات — آلية العمل، المستويات، الحماية، الحل والتقييم

> **التاريخ:** 2026-09-12 — **النطاق:** نظام التحديات طالب فقط (`desktop-app/server` الحي على `:3000`).
> **المنهجية:** قراءة الكود (مسارات:أسطر) + تنفيذ فعلي (DB حية + API حي) + harness آلي. `FACT` = مثبت. `INFERENCE` = استنتاج.
> **الملفات:** هذا التقرير + `CHALLENGES_PER_CHALLENGE.md` (جدول 210 صفًا مولّد آليًا) + سكربتات البحث في `/tmp/opencode/ch-research/` (`audit.js`, `antisweep.js`, `sample.js`, `gen-appendix.js` + نتائج `audit.json`, `antisweep.json`, `sample.json`).
> **تغيرات DB أثناء البحث (موثقة):** بذر 210 تحديًا (`seed.js` بعد إصلاح المؤلف) + تسجيل حساب `researcher67262@t.local` + 6 تقديمات ناجحة عليه + حذف 3 تحديات مؤقتة من دفعة سابقة (أعادت العدد لـ210 بالضبط).

## 0. الخلاصة التنفيذية

- **210 تحديات حية**: 7 مستويات × 30، كلها `published + schoolId:null` (نظامية)، ظاهرة كاملة عبر API (213→210 بعد التنظيف).
- **خط الأنابيب سليم ومُتحقق حيًا**: تقديم → أقفال (مستوى 80% + تسلسلي) → sandbox → تقييم خادم → معاملة ذرية (تقديم + نشاط + XP + يومي + إنجازات).
- **مكافحة الغش صامدة 210/210**: هجوم `طباعة(المخرج)` مرفوض في كل المستويات (0 ثغرات) — بثلاث شبكات متكاملة (hardcoding للمستوى 2+، فاحص الشبهة، متطلبات الكلمات).
- **6 حلول صادقة عبر API**: ناجحة 100/100، +100 XP لكل واحد، عدم ازدواجية مزدوج-التحقق، قفل المستوى 403 مُتحقق.
- **مشكلتان حقيقيتان**: (1) مسار OOP (51 تحديًا، المستويان 4 و7) غير قابل للحل كما هو مكتوب (الباني `جديد` + أسماء محجوزة) — تفصيل §8. (2) انتحال `X-Forwarded-For` يُسجَّل كـ IP حقيقي (جديد، §4.10).
- **ملاحظات عرض**: الواجهة تعرض `XP 10` بينما الخادم يمنح 100 (جدول الصعوبة) — §3.4.

## 1. المخزون: المستويات والأقسام

**FACT** (من `Studio/tests/challenges_tier{1..7}.js` + `challenges-page.js:20-30` + `roadmap-page.js:6-13`):

| المستوى | اسم الواجهة | الموضوع | العدد | الصعوبة الغالبة |
|---|---|---|---|---|
| 1 | أساسيات البرمجة | طباعة/متغيرات/حساب | 30 | BEGINNER |
| 2 | المنطق والتحكم | شرط/تكرار | 30 | BEGINNER→INTERMEDIATE |
| 3 | هياكل البيانات | (ملفات المستوى 3) | 30 | متفاوتة |
| 4 | البرمجة الكائنية | أصناف/بواني | 30 | متفاوتة |
| 5 | الخوارزميات وإدارة الذاكرة | (ملفات المستوى 5) | 30 | متفاوتة |
| 6 | المصفوفات (+المتقدمة في الخارطة) | مصفوفات | 30 | متفاوتة |
| 7 | البرمجة الكائنية المتقدمة | وراثة/تجريد/واجهات | 30 | متفاوتة |

- كل مستوى: ~10 دروس `المثال` (starter = الحل الكامل) + ~20 `تمارين` (starter فارغ غالبًا: 140 فارغة من 210).
- الأنواع في DB: `LESSON` / `EXERCISE` فقط. الصعوبات: `BEGINNER/INTERMEDIATE/ADVANCED/EXPERT`.
- شارات الواجهة للمستوى: مبتدئ/متوسط/متقدم/متمرس/محترف/مخضرم/خبير (`TIER_DIFF`).
- الترتيب داخل المستوى `order: 1..30` متسلسل بلا فجوات ولا تكرار (بعد حذف 3 مؤقتات كانت تكرر 1..3 — §9.5).

## 2. طريقة العمل: خط التقديم الكامل (مُتحقق حيًا)

**FACT** (`src/routes/student.js:176-365` + `studentProfileController.js:68-169`):

```text
POST /api/v1/student/challenge/submit {challengeId, code}
  │ middleware: authenticate → requireStudent → trainingSubmissionRateLimit (10/ساعة)
  ├─ 1. تحقق الأنواع (string/string) وإلا 400  [:182-188]
  ├─ 2. التحدي موجود؟ وإلا 404  [:191-194]
  ├─ 3. قفل المستوى: نسبة إنجاز السابق ≥ 80% (على أول 50) وإلا 403 tierLocked  [:199-229]
  ├─ 4. القفل التسلسلي: السابق في الترتيب ناجح؟ وإلا 403 challengeLocked  [:234-257]
  ├─ 5. عدم الازدواج: ناجح سابقًا؟ → {alreadyPassed:true} بلا XP  [:262-267]
  ├─ 6. Sandbox: executeDhad(code) خارج عملية API  [:278]
  ├─ 7. التقييم: evaluate(code, challenge, studentName, execResult)  [:284]
  │     output 60 + structure 25 + integrity 15 = 100؛ النجاح ≥ 60
  │     + بلا أخطاء + بنية صالحة + غير مشبوه + لا HIGH/CRITICAL + تنفيذ ناجح
  ├─ 8. تعقيم HTML للكود المخزن (<>&"')  [:287-294]
  └─ 9. معاملة ذرية: submission + activityLog + awardXP + dailyXP + achievements  [:301-349]
        → {passed, submissionId, xpAwarded, score, message, codeErrors}  [:351-361]
```

- **أدلة حية** (حساب `researcher67262@t.local`): تسجيل 201 → دخول 200 → قفل مستوى 403 (`tierLocked:true`) → 6 حلول `200 passed score=100 xp=100` → إعادة تقديم `{alreadyPassed:true}` مع ثبات XP (100→100) → الملف `200 totalXP=600 level=4`.

## 3. النقاط والمستويات والقيادة

**FACT** (`studentProfileController.js:12-19, 68-169, 264-271` + تحقق حي):

- **جدول XP حسب الصعوبة**: `BEGINNER:100 / INTERMEDIATE:200 / ADVANCED:300 / EXPERT:500` — من `challenge.difficulty` حصرًا، **لا من حقل `xpReward`** ولا مكافآت سرعة/أداء (محذوفة عمدًا: تعليق `:91-94`).
- **⚠️ عدم تطابق العرض**: الواجهة تعرض شارة `XP 10` (من `xpReward`) بينما الخادم يمنح 100. كل حلول العينة: `xp=100` رغم شارة 10 — §9.3.
- المستوى من عتبات تراكمية (`calculateLevel`) + `streak` يومي + `dailyXP` + فحص إنجازات + سجل `xp_awarded`.
- القيادة: للطالب أفضل 50 في مدرسته؛ للتحليلات Redis `leaderboard:xp` (TTL 5د) مع بديل DB.

## 4. الحماية: عشر طبقات (كلها مُتحقق منها)

| # | الطبقة | الدليل | التحقق الحي |
|---|---|---|---|
| 1 | مصادقة طالب فقط | `authenticate + requireStudent` على كل مسارات الطالب؛ بوابة R1 ترفض غير الطالب | دخول طالب 200؛ (رفض المعلم بـ401 مُختبر jest) |
| 2 | CSRF مزدوج | `csrf.js` كوكي+ترويسة متطابقتين؛ الدخول/التسجيل معفيان فقط | كل تقديمات العينة أرسلت `X-CSRF-Token` ونجحت |
| 3 | حدود المعدل | تقديم التدريب 10/ساعة (`security.js:62-64`)؛ مصادقة 20/15د؛ OTP 3/5 | **اصطدمت بالحد حيًا** (5×`429`) — يعمل |
| 4 | حجب المخرجات | القائمة تُخفي `expectedOutput/dynamicOutput` عن الطالب (`challengeController:277`) | **أُثبت عمليًا**: سكربت العينة افترض وجودها فأنتج `طباعة(undefined)` وفشل — اضطررت لقراءتها من ملفات البيانات |
| 5 | تنفيذ خادم فقط | `executeDhad` + تجاهل أي `output/score` من العميل (تعليق صريح `:173-175` + endpoint القديم `410`) | — (مراجعة كود) |
| 6 | حدود الصندوق | مهلة 5s + سقف مخرجات 64KB (C2) + حظر `constructor/getPrototypeOf` | 55/55 اختبارات الصندوق |
| 7 | كشف التصليب M7 | حرفي مباشر (2+) + دمج/قوالب + `fromCharCode` + base64 حقيقي + المستوى 1 معفي (starter يحوي الحل) | مسح 210 (أدناه) + 5 اختبارات |
| 8 | فاحص الشبهة | `very_short/direct_output/no_computation/trivial` (LOW/MEDIUM تحذير؛ HIGH/CRITICAL رسوب) | طباعة-غش tier-1 مرفوض بمجموعها |
| 9 | متطلبات الكلمات | `must_use_keyword` (النوع الوحيد المستخدم من 33؟ — كل الـ210 بهذا النوع فقط) تجعل `structure.valid=false` عند غيابها | سبب رسوب غش تمارين tier-1 (برهان §6) |
| 10 | تدقيق وسجلات | `CHALLENGE_SUBMISSION_BLOCKED_*` + `LOGIN_*` + `xp_awarded` + قفل IP (M4) | سجلات `dhad-server.log` شهدت محاولاتي |

### 4.10 جديد وخطير: انتحال IP عبر `X-Forwarded-For` (مُثبت حيًا)

- **الدليل الحي**: طلب دخول فاشل مع ترويسة `X-Forwarded-For: 9.9.9.9` سُجل في `dhad-server.log` كـ `"ip":"9.9.9.9"` — أي أن `trust proxy` يعتمد الترويسة من أي عميل مباشر.
- **الأثر (INFERENCE)**: كل حدود/حظر IP (بما فيها M4 الجديد: 20 فشل/ساعة) تُتجاوز بتدوير الترويسة + حوض جديد لكل قيمة. لا يتطلب مصادقة لاستغلاله في تضخيم المحاولات.
- **التوصية**: الثقة بالبروكسي فقط عند وجود بروكسي حقيقي (`trust proxy` بعدد القفزات الصحيح أو إيقافه محليًا) + مفتاح الحدود بهوية أقوى (حساب/جلسة) بجانب IP. (لم يُنفذ — خارج نطاق هذه الدفعة، يُضاف للخطة.)

## 5. الفحص الهيكلي الآلي (210/210)

**FACT** (`audit.json` مولّد من DB الحية):

- منشورة: 210/210، `schoolId:null` (نظامية) 210/210، بلا ترتيب مكرر ولا فجوات (1..30 × 7).
- الصعوبات والأنواع نظامية؛ أنواع المتطلبات: `must_use_keyword` فقط (33 كلمة مميزة، أكثرها `طباعة/صحيح/إذا`؛ فقيرة — لا `min_lines` ولا قواعد مخصصة مستخدمة رغم دعم الكود).
- starters فارغة: 140 (تمارين — **سليم بالتصميم**).
- starters بأخطاء حقيقية: **17** — كلها دروس OOP (المستوى 4: عشرة + المستوى 7: سبعة) — §8.
- مخرجات فارغة: 0. JSON متطلبات سليم: 210/210.

## 6. مسح مكافحة الغش (210/210 — صفر ثغرات)

- **المنهج**: لكل تحدي: `كود الغش = طباعة(المخرج المتوقع حرفيًا)` → تنفيذ حقيقي → `evaluate()` كاملة. متوقع النجاح للمستوى 1 (معفي) والرسوب لما فوق.
- **النتيجة**: **180 رفضًا مصممًا للمستوى 2+ (0 ثغرات)** + 6 نجاحات مصممة (دروس 1-6) + 24 رفضًا مصممًا بشبكة الشبهة/المتطلبات.
- **تفصيل الرفض**: حرفي مباشر (hardcoding) للمستوى 2+؛ و`direct_output/no_computation` + غياب كلمات المتطلبات للمستوى 1 (برهان: نفس الكود المباشر ينجح، والصادق `صحيح x=25/طباعة(x)` ينجح 100).
- **الديناميكية**: تحديات `dynamicOutput` ترفض الغش الثابت (عدم تطابق) — سليم.
- **ملاحظة منهجية صادقة**: المسح الأول أعطى نتائج معكوسة لخطأ في harness (نسيان تمرير نتيجة التنفيذ) — اكتُشف وصُحح وأُعيد التشغيل؛ الأرقام أعلاه من التشغيل المصحح.

## 7. الحلول الصادقة عبر API (جدول)

**FACT** (حساب الباحث، `sample.json`):

| التحدي | الطريقة | الحالة | النقاط | XP |
|---|---|---|---|---|
| طباعة: المثال (1) | starter كما هو | ناجح 100 | 100 | 100 |
| طباعة: تمارين 1 (2) | `طباعة("أهلاً يا عالم")` | ناجح 100 | 100 | 100 |
| طباعة: تمارين 2 (3) | `طباعة("برمجة")` | ناجح 100 | 100 | 100 |
| طباعة أرقام: المثال (4) | starter `طباعة(42)` | ناجح 100 | 100 | 100 |
| طباعة أرقام: تمارين 1 (5) | `طباعة(100)` | ناجح 100 | 100 | 100 |
| طباعة أرقام: تمارين 2 (6) | `طباعة(0)` | ناجح 100 | 100 | 100 |
| إعادة #1 | — | `alreadyPassed` بلا XP إضافي | — | 100→100 |
| مستوى 2 قبل إكمال 80% من 1 | — | `403 tierLocked:true` | — | — |
| الإجمالي | — | — | — | 600 (بما فيها 100 يتيمة من تحدٍّ محذوف — §9.4) |

## 8. فجوة OOP: مسار المستويين 4 و7 غير قابل للحل كما هو مكتوب ⚠️

**FACT** (مسابير حية على المحرك + محتوى الدروس):

| البنية | حالة المحرك |
|---|---|
| `صنف شكل {}` (إعلان فارغ) | ✅ تُترجم وتعمل |
| دوال داخل الصنف + `هذا.` + `ارجع` | ✅ تعمل |
| `يرث` / `عام` / `مجرّد` | ✅ تُترجم (السلوك الدلالي غير مُتحقق) |
| `دالة جديد(...)` (الباني) | ❌ خطأ تحليل (كل دروس T4/T7 تستخدمه) |
| `جديد X` (إنشاء) | ❌ يُترجم ويفشل تشغيلًا (`not a constructor`) |
| `واجهة` | ❌ خطأ تحليل |
| اسم محجوز كاسم صنف (`دائرة` = كلمة رسم!) | ❌ خطأ تحليل من السطر الأول |

- **الحجم**: 51 تحديًا تشترط `صنف/جديد` + 13 `يرث` + 9 `عام` + 3 `واجهة`؛ و17 starter دروس لا تُترجم أصلًا (تُعلّم كودًا مكسورًا، e.g. `صنف دائرة` و`جديد(7)` بلا صنف و`جديد(5، 10)` بفاصلة عربية).
- **الأثر (INFERENCE)**: طالب المستوى 4 لا يستطيع إكمال أي تمرين يشترط `جديد` بأي صياغة — المسار مغلق فعليًا حتى (أ) يُوسَّع المحرك (بواني حقيقية) أو (ب) تُعاد كتابة المحتوى على المجموعة المدعومة.
- **توصية**: تجميد T4/T7 مؤقتًا (`published=false`) أو وسمها، وإصلاح المحرك أو المحتوى — قرار تصميمي مطلوب.

## 9. مشاكل مرتبة (مكتشفة في هذا البحث)

1. **OOP غير قابل للحل (51)** — §8. الأولوية: قرار (توسيع محرك/إعادة محتوى/تجميد).
2. **انتحال XFF (جديد)** — §4.10. الأولوية: إصلاح `trust proxy` + مفتاح أقوى.
3. **XP المعروض ≠ الممنوح**: شارة `XP 10` مقابل منح 100 (جدول الصعوبة). توحيد العرض مع `XP_TABLE`.
4. **بقايا يتيمة**: حذف تحدٍّ لا يمسح تقديماته/نقاطه (ملفي الشخصي يحمل 100 من تحدٍّ محذوف). `onDelete` للتقديمات أو تنظيف دوري.
5. **(أُصلحت أثناء البحث)** ترتيب مكرر من 3 مؤقتات → حُذفت (عاد 210/30 نظيفة)؛ سقف `take:210` → 1000؛ بوابة ADMIN في `seed.js` → مالك النظام.
6. **حد 10/ساعة يُمسح بإعادة التشغيل** (شوهد حيًا) — معروف من H8؛ مع XFF أشد.
7. **رسالة القفل التسلسلي غامضة مع التكرار** (زالت بزوال التكرار).
8. **140 starter فارغة** — سليم، لكن تجربة الطالب: صندوق فارغ بلا تلميح (الوصف يعوّض غالبًا).
9. **أنواع المتطلبات أحادية** (`must_use_keyword` فقط) — تفعيل `min_lines` وقواعد مخصصة يقوّي التقييم.
10. **المثال = الإجابة** (دروس): ناجحة باللصق — مقصود بيداغوجيًا، لكنها تضخم الإنجاز المبكر (6×100 للصق).

## 10. حدود البحث وما لم يُفعل

- تمارين المستويات 2-7 (180) حُللت هيكليًا + رُفض غشها آليًا، لكن لم تُحل يدويًا واحدًا واحدًا (180 مهمة يدوية) — قابلية حلها الفردية (عدا OOP) **INFERENCE** من سلامة المحرك للدوال/الشروط/الحلقات، لا برهان مباشر لكل واحد.
- السلوك الدلالي لـ `يرث/عام/مجرّد` (تُترجم) لم يُختبر تنفيذيًا بعمق.
- حساب الباحث باقٍ في DB (`researcher67262@t.local`) للتدقيق — يُحذف عند الطلب.
- لم يُمس كود الإنتاج في هذا البحث إلا بإصلاحي البذر والسقف الموثقين سابقًا.

## 11. إعادة الإنتاج

```bash
cd Studio/desktop-app/server
export DATABASE_URL="file:$PWD/prisma/dev.db"
node /tmp/opencode/ch-research/audit.js        # هيكلي → audit.json
node /tmp/opencode/ch-research/antisweep.js    # غش → antisweep.json
node /tmp/opencode/ch-research/gen-appendix.js # ملحق الصفوف
node /tmp/opencode/ch-research/sample.js       # عينة HTTP (يحتاج السيرفر حيًا)
```

> التفصيل لكل تحدي: `CHALLENGES_PER_CHALLENGE.md` (210 صفوف: هيكل + غش + حكم).
