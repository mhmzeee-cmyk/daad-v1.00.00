# تقرير تنفيذ توسيع محرك OOP (من الأصعب إلى الأسهل: P3 → P4 → P1 → P2 → P5 → P6 → P7 → P8)

> **التاريخ:** 2026-09-12 — **الأساس:** `OOP_ENGINE_BASELINE.md` (يُحفظ كسجل ما قبل التغيير — لا يُعدَّل).
> **النطاق:** محرك ض JS (4 نسخ: `Web/js` + `frontend-web/js` + `desktop-app/server/src/utils/dhad` + `Full/server/src/utils/dhad`) + محتوى `Studio/tests/challenges_tier{4,7}.js` + صفوف DB.
> كل حزمة طُبقت على النسخ الأربع مع اختبارات دائمة (شجرتا السيرفر).

## 0. النتيجة بالأرقام

| المؤشر | قبل | بعد |
|---|---|---|
| دروس T4/T7 المثال تُترجم وتُخرج المتوقع (20) | 3/20 | **20/20** ✅ |
| مسح الغش (210, طباعة-المخرج) | 0 ثغرات | **0 ثغرات** (لا انحدار) ✅ |
| المجموعة الكاملة | 716/716 | **734/734** (+18 اختبار OOP) ✅ |
| حل OOP حي عبر API | — | `passed score=100 xp=100` ✅ |
| تطابق النسخ الأربع | — | مُتحقق `diff` + `mirror.js` 7/7 ✅ |

## 1. P3: كلمات سياقية (الأصعب) ✅

- **المشكلة**: `دائرة/مستطيل/خط/صورة/قالب` تُلكس ككلمات رسومية (`KW_CIRCLE`...) فتفشل كأسماء أصناف (مُثبت بتفريغ lexer).
- **الحل**: `expectClassName()` جديد في المحلل يقبل `IDENTIFIER` + الكلمات الخمس **فقط بعد `صنف/يرث/جديد`** (4 مواضع: اسم الصنف، الأب، `new` العباري، `new` التعبيري) — بلا أي تغيير في الليكسر، فتبقى جمل الرسوميات كما هي.
- **ملفات**: `dhad-parser.js` ×4 (مساعد + 4 استبدالات لكل).
- **التحقق**: `صنف دائرة + جديد دائرة()` → `1` حيًا؛ المجموعة خضراء (لا انحدار رسوميات)؛ 8 اختبارات P3 (5 أسماء + وراثة + رسالة + تشغيل).
- **درس مستفاد**: `صحيح خط = 5` تُقبل أصلًا (المواضع الضيقة فقط كانت مكسورة) — لذا رُفض توسيع `expectName` العام.

## 2. P4: `واجهة` + `مجرّد` ✅

- **التصميم**: `واجهة Name { sig... }` تُبنى كهيكل صنف فارغ (يُصدَّر `function Name(){}`) فتعمل سلاسل `يرث` والتجاوز حقيقيًا (مُثبت: `كتاب` → `كتاب`); التوقيعات تُتحقق أقواسها وتُهمل عمدًا (موثق). `مجرّد` علامة موثقة بلا أثر تشغيلي (الإنفاذ عمل مستقبلي).
- **ملفات**: `dhad-parser.js` ×4 (توجيه + `parseInterface` + `parseAbstract`).
- **التحقق**: وراثة واجهة + تجاوز → `كتاب` حيًا؛ 3 اختبارات P4.

## 3. P1: بانٍ بصيغة المحتوى `دالة جديد(...)` ✅

- **الحل**: فرع جديد في `parseClass` يحوّل `دالة جديد` إلى `ConstructorDeclAST` حقيقي، بمعاملات مرنة (منمطة أو عارية بافتراضي `صحيح` أو بقيم افتراضية — نفس تساهل الدوال العادية، بما فيها `name =`).
- **الدليل**: starter الدرس T4#1 حرفيًا → `43.96` (صحيح رياضيًا: 2×3.14×7).
- **ملفات**: `dhad-parser.js` ×4. **التحقق**: 3 اختبارات P1 (عارٍ/منمط/فارغ).

## 4. P2: رسالة المصنعي ✅

- `جديد(...)` بلا نوع مستحيل دلاليًا (لا صنف للاستدلال) — القرار: رفض واضح بدل الصمت: `متوقع: اسم صنف بعد صنف/يرث/جديد (مثال: جديد مستطيل())`.
- **ملفات**: `dhad-parser.js` ×4 + اختبار P2 ×2.

## 5. P5: إصلاح الصمت الخطر ✅

- **المشكلة**: `دالة` بنفس اسم الصنف كانت تُبنى دالة عادية فيتجاهلها `new` → `عدم` بلا خطأ (أُثبتت بتفريغ AST: `FunctionDecl` مقابل `ConstructorDecl`).
- **الحل**: تحويلها لباني في `parseClass` (4 ملفات) + اختبار (`7` بدل `عدم`).

## 6. P6: مسار المحتوى + إصلاحا محرك عميقان اكتُشفا أثناء الترحيل ✅

### 6.1 ترحيل الدروس (17 starter في `challenges_tier{4,7}.js` + مزامنة 60 صف DB بالعنوان)

| الإصلاح | الدروس | ملاحظة |
|---|---|---|
| `جديد(...)` → `جديد ClassName(...)` | 10 دروس | كل مصنعي بلا نوع |
| `دالة حالة()` → `دالة التقدير()` (+ الاستدعاء + نص التمرين) | T4#16 | `حالة` كلمة شرط محجوزة؛ المتطلبات لا تشترطها |
| `عام دالة مساحة()` العارية → `{}` | T7#13 | دوال مجردة بلا أجسام غير مدعومة |
| `جديد(5، 10)` → `جديد نقطة()` + إسنادات (فاصل `؛` يعمل) | T7#28 | لا مصنعي للهياكل |
| `أمسك` → `امسك` (4 مواضع) + alias ليكسري | T7#25 + تمارين | **ملاحظة**: `امسك` كانت موجودة أصلًا؛ أُضيف `أمسك` كمرادف للمحرك + وُحّد المحتوى على `امسك` (لأن الفاحص نصي) |
| `طباعة(النتيجة)` الوهمية | لم توجد أصلًا (سوء قراءة مبدئية مُصحح) | — |
| `мяو` سيريلي بدل `مواء` | **تُرك عمدًا** (متسق starter/متوقع؛ تغييره لاحقًا مع مراجعة المحتوى) | — |

### 6.2 إصلاحا محرك اكتُشفا بالترحيل (الأهم تقنيًا)

- **(أ) `obj.field[i] = v` كان يُسقط الفهرس بصمت** (`parser.js:1220`: يُستهلك ولا يُغلَّف لغير المتغير → كتابة فوق الحقل كله). **الحل**: تغليف `ArraySubscriptExpr(object)` + توليد ثنائي النوع في `codegen.js` (قراءة+كتابة). الدليل: `سعود×4` → `أحمد/سعود/2`.
- **(ب) الحقول الخاصة: كتابة `this.X` وقراءة `_X`** (عدم دائم). **الحل**: مجموعة `_privateNames` لكل صنف + توجيه القراءة/الكتابة لمتغير الإغلاق. الدليل: `1000` ✅.
- **(ج) معاملات الباني/الدوال تظلل الحقول** (`this.X` تُقرأ بدل المعامل). **الحل**: استبعاد أسماء المعاملات من `classFields` (بانٍ ودوال). الدليل: T7#7 + T7#13.
- **(د) `M6c` ملاحظة**: `هذا.طول/حجم` يُترجمان `.length` (مقصود للنصوص/المصفوفات) — يُمنعان كأسماء حقول (أُعيدت تسمية `الطول`→`الضلع` في T7#13)؛ `طول()` كدالة سليم. **قاعدة تأليف موثقة**: لا حقول بأسماء `طول/حجم`.

### 6.3 درسان أُعيدت كتابتهما جوهريًا

- **T7#19 (النطاق)**: خطأ مطبعي `طباعة(النتيجة)` بدل `نتيجة` (المتغير) — أُصلح (وهو سبب عدم لا المحرك!).
- **T7#25 (try/catch)**: قسمة JS على صفر لا ترمي (`Infinity`) — أُعيد starter لـ `ارمِ` صريح (يثبت آلية الالتقاط، والمخرج المتوقع ثابت).

## 7. P7 + P8 ✅

- مثال المحرر (`فئة` + `دالة()` مجهولة + `جديد x =`) أُعيد بلهجة مدعومة مثبتة التشغيل (`صوت الحيوان`) ×3 مرايا.
- البوابة الدائمة: P1/P3/P4/P2/P5/P6 describes في `webTranspiler.test.js` (×2) + سكربتات `/tmp/opencode/oop-probe/` (`probe.js`, `mirror.js`, `verify-lessons.js`) كبوابة يدوية موثقة في التقرير الأساسي.

## 8. دروس مستفادة (عملية)

1. **استبدال النص الجزئي خطير**: `عام دالة مساحة()` طابقت نسخة had-brace فأنتجت `{} {` — اكتُشف بفحص البايت (20→19/20). القاعدة: تحقق لاحق `grep` + تشغيل، لا ثقة عمياء.
2. **أخطاء المحتوى تتنكر كأخطاء محرك**: `النتيجة/نتيجة`، `b/ب` المختلطة — التفريغ البايتي حسمها.
3. **الاختبارات الصامتة أخطر من الفاشلة**: R5 وsubscript وprivate كانت تنتج قيمًا خاطئة بلا أخطاء — لذا كل إصلاح رافقه اختبار قيمة لا مجرد ترجمة.
4. **لا `stash` مع عمل غير محفوظ** (درس سابق مؤكد مجددًا) — النسخ إلى `/tmp` قبل أي عملية git خطرة.

## 9. المتبقي المقترح (خارج هذا النطاق) — محدَّثة 2026-09-12

- ~~`مجرّد` إنفاذ حقيقي~~ ✅ مُنفَّذ (Item-5). **تحديث 2026-09-12 Hotfix:** حارس `this.constructor` كان يفشل في Sandbox (Proxy يحجب `constructor` → `undefined`)، استُبدل بـ `new.target && new.target.name === 'ClassName'` (كلمة مفتاحية لا تمر عبر Proxy).
- ~~`خاص` على الدوال (تُقبل نحويًا بلا إنفاذ)~~ ✅ مُنفَّذ (Item-5): تُحوَّل لمتغيرات إغلاق.
- ~~`طول/حجم` كحقول: توجيه الكتابة~~ ✅ مُنفَّذ (Item-5): `throw` في الباني عند استخدامهما كأسماء حقول.
- ~~`мяو` السيريلي~~ ✅ مُصحَّح (Item-5): `مواء` عربي بدل `мяو` سيريلي.
- `واجهة` بلا `يرث`؟ (تعمل كصنف فارغ — مقبول).
- `مجرّد`-only + `this` خارج الأصناف: سلوكيات طرفية غير مفحوصة.
- تمارين T4/T7 الـ40: حُللت هيكليًا ورُفض غشها؛ الحل اليدوي الفردي لكل واحد خارج النطاق (العينة + الدروس كافية كدليل مسار).
