# T10g: Label+offset — تعبيرات مع عناوين
# اختبار حلقة تكرارية مع عدّاد
load s0, 5      # س0 = عدّاد = 5
load s1, 3      # س1 = قيمة = 3
load acc, 0     # مح = نتيجة = 0
loop:
add s1          # مح += 3
dec s0          # س0--
jnz loop        # إذا س0 != 0 كرر
# النتيجة: مح = 3*5 = 15
halt
