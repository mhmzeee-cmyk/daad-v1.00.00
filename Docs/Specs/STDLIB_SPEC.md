# مواصفات المكتبة القياسية للغة ض — من الكود الفعلي فقط

> **المصادر:** `Compiler/stdlib/{DaadStdlib.hpp:263,DaadStdlibExt.hpp:2141,DaadGraphics.hpp:398,Math.hpp:6,String.hpp:5,Runtime/DaadRuntime.hpp:53}` + `Compiler/stdlib/*/*.ض` (10 مجلدات × 10 ملفات = 100 ملف، **2861** تعريف `دالة` إجمالًا بما فيها المولدة) + `Compiler/stdlib_arabic/` (10 مجلدات، 100 ملف) + `Studio/Web/js/dhad-libraries.js:2765` (15 فئة فعالة، **264** تعريفًا) + `Studio/Web/js/dhad-codegen.js:86-223` (شيم runtime) + `Compiler/src/CodeGen.cpp:110-148` (قائمة `using` الانتقائية: 34 اسمًا).
> **منهجية العد:** `grep -c` + `regex دالة\s+([^\s\(]+)` لكل ملف. الأسماء المولدة آليًا (`الحد_الأعلى_N`/`الحد_الأدنى_N`/`الفارق_N` — تضخيم `expand_utils`-style) **مفصولة ومعدودة وحدها** ولا تُحسب دوال حقيقية. لا تخمين: كل اسم أدناه مقروء فعليًا.

## 0. الخلاصة الرقمية (FACT)

| المخزن | الحجم | الفريد الحقيقي (بلا مولدة) | المولدة/الوهمية |
|---|---|---|---|
| `Compiler/stdlib/*.hpp` (5) | 2866 سطرًا | ~40 دالة حقيقية (`DaadStdlib` 34 + `Ext` 11 − تداخل، + `Graphics`/`Math`/`String`/`Runtime`) والباقي `template return 0` stubs (~200 في Ext وحدها) | ~200+ stub (`DaadStdlib.hpp:222-247` + `Ext:85-2128`) |
| `Compiler/stdlib/*/*.ض` (100) | **2861** تعريف `دالة` | **~754** اسمًا فريدًا عبر المجلدات (1041 مجموع الفريد لكل مجلد مع التكرار البيني) | **~287** اسمًا مولدًا (`الحد_*/الفارق_*`) + ملفات حشو 10-دوال مكررة (`طول_نص/بحث_موضع/هل_يحتوي...` في 8+ ملفات) |
| `Compiler/stdlib_arabic/` (100) | 100 ملف (مرآة عربية) | مرآة — لم تُقارن سطرًا بسطر هنا (UNKNOWN التطابق الكامل) | — |
| `dhad-libraries.js` (JS) | 2765 سطرًا، 15 فئة فعالة (16 مفتاحًا، `الأمان` مكررة تُسقط الأولى) | **264** تعريفًا (بما فيها 1 متداخلة `حلقة`) | رأس كاذب (`10 categories, 90 files, 600+ funcs:4-6` — الفعلي 15 فئة و264) |
| التقاطع (نفس الاسم العربي) | — | **~30** دالة (القائمة في §3) | — |

## 1. دوال C++ الحقيقية في `.hpp` (القائمة الكاملة — بلا stubs)

### 1.1 `Compiler/stdlib/DaadStdlib.hpp` — 34 حقيقية + 6 قوالب حقيقية (FACT: الأسطر)

```cpp
// DaadStdlib.hpp:20  double جذر(double x)                      — sqrt
// :21                 int مضروب(int n)                          — حلقة
// :26                 string اقرأ_stdin()                        — getline(cin)
// :40                 int طول(const string&)                     — length
// :42                 string استخرج(const string&, int من, int إلى) — substr
// :46                 int ابحث(const string&, استعلام)            — find/-1
// :51                 string استبدل(const string&, قديم, جديد)    — replace loop
// :61                 string صغير(const string&)                  — tolower
// :67                 string كبير(const string&)                  — toupper
// :73                 string نظف(const string&)                   — trim
// :79                 bool يبدأ_بـ(const string&, بادئ)           — compare
// :83                 bool ينتهي_بـ(const string&, لاحق)          — compare
// :88                 vector<string> قسّم(const string&, فاصل)     — split
// :106                double قوة(double أصل, double أس)          — pow
// :107                double مطلق(double x)                      — abs
// :108                int أقصى(int a, int b)                     — max
// :109                int أدناه(int a, int b)                    — min
// :113                double عشوائي()                            — mt19937 [0,1)
// :125                int حجم_المصفوفة(const vector<int>&)       — size
// :126                void أضف_لمصفوفة(vector<int>&, int)         — push_back
// :127                int احصل_من_مصفوفة(const vector<int>&, int) — m[i]
// :128                void عّين_في_مصفوفة(vector<int>&, int, int) — assign
// :129                void احذف_من_مصفوفة(vector<int>&, int)       — erase
// :130                void قلب_المصفوفة(vector<int>&)             — reverse
// :131                void فرّغ_المصفوفة(vector<int>&)            — clear
// :132                vector<int> انسخ_مصفوفة(const vector<int>&)  — copy
// :133                vector<int> ادمج_مصفوفتين(const vector<int>&, const vector<int>&)
// :140                int طول_مصفوفة(const vector<int>&)           — حجم_المصفوفة
// :142 (قالب)       int طول_مصفوفة(const vector<T>&)
// :144 (قالب)       int طول(const vector<T>&)
// :146                vector<int>& أضف(vector<int>&, int)         — push+ref
// :149 (قالب)       vector<T>& أضف(vector<T>&, const T&)
// :151 (قالب)       vector<T>& أضف_وأعد(vector<T>&, const T&)
// :154 (قالب)       void أضف(vector<int>&, const A0&, const A1&) — push 0 (توافقية)
// :155                void احذف_من(vector<int>&, int)             — احذف_من_مصفوفة
// :156                void عكس_مصفوفة(vector<int>&)                — قلب_المصفوفة
// :157                int ابحث_في(const vector<int>&, int)         — linear/-1
// :205                void اكتب_stdout(const string&)             — cout
// :209                void اكتب_stderr(const string&)             — cerr
// :250-260 أسماء بديلة/أنواع: قسّم، عّين_في_مصفوفة، فرّغ_المصفوفة، مصفوفة=مجموعة=قائمة=vector<int>، لون=string، مدة=تأخير=int
// STUBS (ليست دوال): :31-33 (جذر/مضروب/اقرأ_stdin variadic) + :222-247 (variadic طول/استخرج/... return 0/""/{}/false)
```

### 1.2 `Compiler/stdlib/DaadStdlibExt.hpp` — 11 حقيقية (FACT: `:1-83`) ثم stubs

```cpp
// Ext:13  long long افتح(const string& مسار, const string& وضع)  — fopen + فهرس جدول
// :18     int سجّل(long long h, const string& سطر)               — fputs + newline
// :25     string اقرأ_سطر(long long h)                           — fgets 4096 + trim \r\n
// :36     vector<string> فصل(const string& نص, const string& فاصل_)
// :48     bool ليس_نهاية(long long h)                            — fgetc/ungetc
// :56     void أغلق(long long h)                                 — fclose
// :60     bool يحتوي(const string& كامل, const string& جزء)     — find
// :63     string اقتطع(const string&, int بداية, int طول_فرعي)   — substr واعٍ بـ UTF-8
// :79     bool بحث_نمط(const string&, const string&)             — find
// :82     string تحويل(int قيمة)                                 — daad_to_string
// STUBS :85-2128 — قالب واحد مكرر ~200 مرة:
//   template <typename... A> inline <Ret> <Name>(A...) { return <0|0.0|""|{}>; }
//   أمثلة: addClass، addEventListener، avg_sql، count_sql، fetch_json، getElementById، GROUP_BY، join_sql...
```

### 1.3 `Compiler/stdlib/DaadGraphics.hpp` + `Math.hpp` + `String.hpp` + `Runtime/DaadRuntime.hpp` (FACT)

```cpp
// DaadGraphics.hpp:19 namespace daad::graphics (حتى :398):
//   :25 struct Color{r,g,b; ==,!=}؛ :34-50 colors (aswad,abyad,ahmar,akhdar,azraq,asfar,burtuqali,banafsaji,samawi,wardi,ramadi,ramadi_fatih,ramadi_dakin,boni,nayli,zumurudi)
//   :52 from_name(string) (+hex #RRGGBB :68-71)؛ :79 class Canvas{w,h,pixels,fg,bg; create:88, at:94/100, set:106}
//   :116 engine() singleton؛ :125 ansi{move_to,clear_screen,hide/show_cursor,bg/fg_rgb,reset}
//   :145 setCanvasSize، :149 setColor(r,g,b)، :153 setColor(string)، :157 setBgColor، :161 clear، :166 rectangle، :174 strokeRect
//   :187 circle، :196 strokeCircle، :207 line (Bresenham)، :223 pixel(x,y)، :227 pixel(x,y,r,g,b)، :231 fill، :238 background
//   :247 render، :271 FONT_5x7، :333 text(string,x,y,scale=1)، :359 saveImage، :374 loadImage، :395 canvasWidth، :396 canvasHeight
// Math.hpp:4 namespace daad::math { :5 جيب(double)->sin؛ :6 جذر(double)->sqrt }
// String.hpp:4 namespace daad::string { :5 طول(const string&)->size_t }
// Runtime/DaadRuntime.hpp:19 namespace daad::runtime {
//   :21-23 daad_to_string(string|char*|char)؛ :24 bool→"صواب"/"خطأ"؛ :27 arithmetic عبر ostringstream؛
//   :37 daad_print_impl (cout — مع فروع ANDROID/APPLE)؛ :48 daad_print(Args&&...) ضم fold }
```

### 1.4 قائمة `using` الانتقائية (34 اسمًا — FACT `CodeGen.cpp:110-148`)

```
طول_مصفوفة، أضف، احذف_من، عكس_مصفوفة، ابحث_في، جذر، مضروب، اقرأ_stdin، طول، استخرج،
ابحث، استبدل، صغير، كبير، نظف، يبدأ_بـ، ينتهي_بـ، قسّم، قوة، مطلق، أقصى، أدناه، عشوائي،
حجم_المصفوفة، أضف_لمصفوفة، احصل_من_مصفوفة، عّين_في_مصفوفة، احذف_من_مصفوفة، قلب_المصفوفة،
فرّغ_المصفوفة، انسخ_مصفوفة، ادمج_مصفوفتين، اكتب_stdout، اكتب_stderr
```
**القاعدة** (`Compiler.cpp:52-62` + `CodeGen.cpp:157-165`): تُرفع تلقائيًا ما لم يعرّفها المصدر (منع التصادم — SEMANTIC R-04).

## 2. دوال `.ض` حسب المجلد (100 ملف — 2861 تعريفًا، 754 فريدًا حقيقيًا + 287 مولدًا)

> **طريقة القراءة:** `regex دالة\s+([^\s\(]+)` لكل ملف. **المولدة** = `الحد_الأعلى_N`/`الحد_الأدنى_N`/`الفارق_N` (تضخيم آلي — تُعد ولا تُوثق كميزات). **الحشو** = ملفات الـ10-دوال المكررة (`طول_نص/بحث_موضع/هل_يحتوي/استبدال_نمط/فتح_ملف/سجل_سطر/افتح_لقراءة/افتح_للكتابة/هل_يحتوي2/طول2`) المنسوخة في 8+ ملفات عبر `io/text/networking/math/time` — مثبتة بالتشابه الحرفي. أدناه لكل مجلد: الملفات (10) + الإجمالي + الفريد الحقيقي (كامل الأسماء) + المولد.

### 2.1 `concurrency/` — 363 تعريفًا | فريد حقيقي 100 | مولد 28

ملفات (10): `actor_pattern(37)، advanced_sync(35)، futures_and_promises(37)، group_sync(34)، locks_and_mutex(35)، message_passing(38)، semaphores(36)، state_machine(37)، threads(37)، work_pool(37)`.
الفريد الحقيقي (100): _then، catches، أبلغ_الحالة، أرسل_لقناة، أرسل_للممثل، أضف_بآمان، أقفل، أقفل_mutex، أكمل_موعود، إجمالي_bar، إحصائيات_الآلة، إحصائيات_القنوات، إحصائيات_المجموعات، إحصائيات_المزرعة، إحصائيات_الممثلين، إرسال_للمزرعة، إرسال_متعدد، إشارة، إضافة_انتقال، إضافة_نهاية، إعادة_تشغيل، إعادة_تعيين، إعادة_تعيين_bar، إعادة_تعيين_semaphore، إعادة_توجيه، إغلاق_القناة، إلغاء_المهمة، إنشاء_bar، إنشاء_mutex، إنشاء_rwlock، إنشاء_semaphore، إنشاء_حالة، إنشاء_حالة_آلة، إنشاء_خيط، إنشاء_عقدة، إنشاء_قفل، إنشاء_قناة، إنشاء_قناة_متعددة، إنشاء_مجموعة_متزامنة، إنشاء_محلل، إنشاء_مزرعة، إنشاء_مستقبل، إنشاء_ممثل، إنشاء_موعود، إنهاء_الخيط، إنهاء_الممثل، إيقاف_الخيط، إيقاف_المزرعة، احذف_بآمان، استقبال_متعدد، استقبل_من_القناة، استقبل_من_الممثل، افتح_mutex، افتح_rwlock، افتح_القفل، اقرأ_rwlock، اقرأ_عقدة، اقرأ_محلل، اكتب_عقدة، اكتب_محلل، الانتظار_الكل، الحالة_الحالية، انتظار، انتظار_المزرعة، انتظر_bar، انتظر_الحالة، انتظر_الخيط، انتظر_المستقبل، بدء_الخيط، تشغيل_الآلة، تعيين_البداية، تفريغ_بآمان، جاهز_المستقبل، حال_الموعود، حالة_الخيط، حالة_الممثل، حاول_القفل، حجم_القناة، حجم_المزرعة، حجم_بآمان، خيط_الحالي، خيط_الرئيسي، خيط_معلّق، دمج_بآمان، رسم_الآلة، صندوق_الممثل، عدد_الخيوط، عدد_الممثلين، عملية_ذراعية، فارغة_القناة، فشل_موعود، قيمة_semaphore، قيمة_المستقبل، محاولة_semaphore، مدير_المزرعة، مسح_الممثلين، مقبول_الآلة، مهام_نشطة، نسخ_بآمان، يحتوي_بآمان.
عينة توقيع (`threads.ض:6`): `دالة إنشاء_خيط(دالة نص) -> صحيح` — ملاحظة: معاملات من نوع `دالة` غير مثبتة التوليد (UNKNOWN).

### 2.2 `data_structures/` — 312 تعريفًا | فريد حقيقي 88 | مولد 28

ملفات (10): `adv_lazy_structures(10)⚠️حشو، adv_stacks_lists(38)، binary_trees(38)، dynamic_arrays(33)، graphs(38)، heaps_stacks(38)، lazy_structures(36)، linked_lists(10)⚠️حشو، maps_sets(34)، tables_folders(37)`.
الفريد الحقيقي (88): .peek_المكدس، BFS، DFS، أخرج_الكوّة، أخرج_المكدس، أدخل_الكوّة، أدخل_دورة، أدخل_للكوم، أدرج_في_الشجرة، أصغر، أضف_hash، أضف_حافة، أضف_صف، أضف_عقدة، أضف_لخريطة، أضف_لمجموعة، أضف_لمصفوفة، أضف_مزدوجة، إدراج_bin، إنشاء_جدول، إنشاء_خريطة، إنشاء_دورة، إنشاء_رسم، إنشاء_شجرة، إنشاء_شجرة_bin، إنشاء_قائمة_مزدوجة، إنشاء_كوم_أولوية، إنشاء_كوّة، إنشاء_مجموعة، إنشاء_مصفوفة، إنشاء_مكدس، ابحث_في_الجدول، ابحث_في_الشجرة، احذف_hash، احذف_صف، احذف_من_الشجرة، احذف_من_خريطة، احذف_من_مصفوفة، احصل_hash، احصل_على_صف، احصل_من_خريطة، احصل_من_مصفوفة، ادفع_المكدس، ادمج_مصفوفتين، استبدال_نمط، استخرج_دورة، استخرج_من_الكوم، اطبع_الأشجار، افتح_لقراءة، افتح_للكتابة، العرض، العمق، القيم، المفاتيح، انسخ_hash، انسخ_مصفوفة، بحث_موضع، تصدير_الجدول، حجم_hash، حجم_الخريطة، حجم_المصفوفة، حجم_المكدس، حذف_عقدة، حلقة_موجودة، دمج_مجموعات، سجل_سطر، شجرة_متوازنة، طول2، طول_نص، عدد_الأعمدة، عدد_الحافات، عدد_الصفوف، عدد_العقد، عدد_العناصر، عضو_في_المجموعة، عّين_في_مصفوفة، فارغ_المكدس، فتح_ملف، فرز_الجدول، فرّغ_الجدول، فرّغ_المصفوفة، قلب_المصفوفة، مسافة_بين، مسح_hash، مسح_المكدس، مفتاح_موجود، هل_يحتوي، هل_يحتوي2.
عينة (`dynamic_arrays.ض:6`): `دالة إنشاء_مصفوفة() -> صحيح`.

### 2.3 `database/` — 370 تعريفًا | فريد حقيقي 95 | مولد 30

ملفات (10): `advanced_queries(38)، caching(37)، connection_clustering(37)، migration_update(38)، nosql_serializations(38)، reading_writing(38)، reports_analysis(36)، schemas_migration(38)، sql_commands(35)، transactions_security(35)`.
الفريد الحقيقي (95): AVG، COUNT، HAVING، JOIN، MAX، MIN، SUM، Savepoint، آخر_سجل، أضف_للتخزين، أعد_للتجمع، أغلق_قاعدة، أول_سجل، إحصائيات_التجمع، إحصائيات_التخزين، إحصائيات_المجموعة، إحصائيات_المخطط، إحصائيات_المعاملات، إحصائيات_الهجرات، إحصائيات_شاملة، إدراج_صف، إدراج_مستند، إضافة_جدول_للمخطط، إغلاق_الاتصال، إغلاق_التجمع، إلغاء_المخطط، إلغاء_معاملة، إلغاء_هجرة، إنشاء_اتصال، إنشاء_تجمع، إنشاء_تخزين، إنشاء_تقرير، إنشاء_جدول_SQL، إنشاء_سجل، إنشاء_قاعدة، إنشاء_مجموعة، إنشاء_مخطط، إنشاء_هجرة، احصل_من_التجمع، احصل_من_التخزين، استعادة_البيانات، استعلام_معقد، استيراد_التخزين، استيراد_المخطط، افتح_قاعدة، الإصدار، التزام_معاملة، انتهاء_الصلاحية، بحث_سجلات، بحث_مستندات، بدء_معاملة، تجمع_متاح، تحديث_سجل، تحديث_صف، تحديث_مستند، تحقق_من_الهجرات، تصدير_التخزين، تصدير_التقارير، تصدير_الجدول، تصدير_المجموعة، تصدير_المخطط، تطبيق_المخطط، تقرير_CSV، تقرير_HTML، تقرير_JSON، تقرير_PDF، جدول_dinamico، حالة_الاتصال، حالة_المعاملة، حجم_التجمع، حجم_التخزين، حذف_جدول_SQL، حذف_جدول_من_المخطط، حذف_سجل، حذف_صف، حذف_مستند، حذف_من_التخزين، سجل_الهجرات، صف_عشوائي، عدّ_السجلات، عدّ_المستندات، فتح_قفل_الجدول، فرز_المستندات، قراءة_سجل، قراءة_مستند، قفل_الجدول، مستوى_العزل، مسح_التخزين، مقارنة_المخطط، ملخص_البيانات، نسخ_البيانات، نفّذ_استعلام، نفّذ_هجرة، هجرات_معلّقة، هجرة_تلقائية.
عينة (`sql_commands.ض:6`): `دالة إنشاء_قاعدة(اسم نص) -> صحيح`.

### 2.4 `graphics/` — 336 تعريفًا | فريد حقيقي 99 | مولد 29

ملفات (10): `3d_templates(37)، animations(36)، colors_themes(10)⚠️حشو، drawing_graphics(35)، image_processing(39)، multimedia(36)، report_printing(34)، templates_ui(37)، themes_styling(36)، tools_helpers(36)`.
الفريد الحقيقي (99): clipboard، filtre، إحصائيات_الفيديو، إحصائيات_المشهد، إشعاع، إضافة_إدخال، إضافة_إضاءة، إضافة_جدول_للتقرير، إضافة_زر، إضافة_صورة_للتقرير، إضافة_قائمة، إضافة_كائن، إضافة_مفتاح، إضافة_مقطع، إضافة_نص، إضافة_نص_للتقرير، إطار_الرسوم، إطار_رسوم_ثابت، إعادة_الثيم، إغلاق_النافذة، إنشاء_تقرير_طباعة، إنشاء_ثيم، إنشاء_رسوم، إنشاء_لوحة، إنشاء_مشهد، إنشاء_نافذة، إنهاء_الرسوم، إيقاف_الرسوم، إيقاف_صوت، إيقاف_فيديو، احفظ_لوحة، ارسم_خط، ارسم_دائرة، ارسم_مستطيل، ارسم_نص، استبدال_نمط، استخراج_الصوت، استيراد_الثيم، افتح_لقراءة، افتح_للكتابة، الثيم_الحالي، بحث_موضع، بدء_الرسوم، تتبع_الرسوم، تحديث_النافذة، تحريك_الكائن، تحريك_الكاميرا، تحريك_المؤشر، تحميل_صورة، تحويل_الصيغة، تحويل_الفيديو، تخصيص_خط، تخصيص_لون، تدوير_الكائن، تشغيل_فيديو، تصدير_HTML، تصدير_PDF، تصدير_الثيم، تصغير، تطبيق_ثيم، تعديل_اللون، تعيين_الكاميرا، تكبير، تكبير_الكائن، تنسيق_الذيل، تنسيق_الرأس، ثيمات_متوفرة، حالة_الرسوم، حدث_زر، دوران، سجل_سطر، سرعة_الرسوم، صوت، صوت_فيديو، صورة_من_فيديو، طباعة_التقرير، طمس، طول2، طول_نص، عرض_اللوحة، عرض_المشهد، عرض_النافذة، عيّن_clipboard، فتح_ملف، قص، قص_فيديو، كشف_الحواف، لوحة_المفاتيح، مؤشر_الفأرة، مدة_الفيديو، مساحة_النافذة، مسح_الثيم، مسح_اللوحة، معلومات_الصورة، ملء_لوحة، نص_إدخال، نقر_الفأرة، هل_يحتوي، هل_يحتوي2.

### 2.5 `io/` — 232 تعريفًا | فريد حقيقي 60 | مولد 28

ملفات (10): `colors_styling(34)، compression_extract(10)⚠️، file_search(10)⚠️، logging(10)⚠️، reading_writing(36)، root_inputs(38)، send_receive(37)، stdin_stdout(10)⚠️، terminal_inputs(37)، translation_errors(10)⚠️`.
الفريد الحقيقي (60): أخضر، أرسل، أرسل_ملف، أزرق، أصفر، إحصائيات، إعادة_تعيين، احصل_على_البيئة، احصل_على_المحتويات، احصل_على_المعاملات، احصل_على_معامل، احمر، استبدال_نمط، استخرج_بكلمة_مرور، استخرج_ملف، استقبل، استقبل_ملف، اسم_البرنامج، اضغط_بكلمة_مرور، اضغط_مجلد، اضغط_ملف، اعرض، اعرض_خطأ، اعرض_سطر_جديد، اعرض_والإدخال، افتح_لقراءة، افتح_للكتابة، اقرأ_رقم، اقرأ_سطر، اقرأ_عشري، امسح_الشاشة، امسح_المؤقت، بحث_موضع، تحقق_من_الأرشيف، تحقق_من_الجاهزية، تحقق_من_الوصول، حالة_الاتصال، حجم_البيانات، حجم_بعد_الضغط، سجل_سطر، طول2، طول_نص، عدد_الإدخالات، عدد_البيئة، عدد_المعاملات، عيّن_البيئة، فتح_ملف، قطع، مسار_البرنامج، مسار_العمل، معامل_موجود، ملفات_مضغوطة، نسبة_الضغط، نص_تحته_خط، نص_خلفية، نص_عريض، نص_مائل، نص_ملون، هل_يحتوي، هل_يحتوي2.

### 2.6 `math/` — 250 تعريفًا | فريد حقيقي 66 | مولد 31

ملفات (10): `basic_operations(34)، combos_probabilities(10)⚠️، complex_numbers(34)، descriptive_geometry(33)، functions_analysis(10)⚠️، math_matrices(41)، numerical_analysis(10)⚠️، percentage(34)، roots_powers(34)، statistical_aspects(10)⚠️`.
الفريد الحقيقي (66): أدناه، أس، أس_e، أس_تربيعي، أس_مكعب، أسس_عشرية، أقصى، أُس_مركب، إضافة_نسبة، استبدال_نمط، افتح_لقراءة، افتح_للكتابة، باقي، بحث_موضع، تخيلي، جداء_متقاطع، جداء_نقطي، جذر، جذر_nth، جذر_تربيعي، جذر_مكعب، جزء، جمع، جمع_مركبين، حجم_المركب، حقيقي، حوّل_إلى_نسبة، خصم، ربح، زاوية، سجل_سطر، ضرب، ضرب_برقم، ضرب_مركبين، ضريبة، طرح، طول2، طول_نص، عدد_مركب، عقد، عكس، فتح_ملف، قسمة، قوة، قوة_مركبة، لوغاريتم، لوغاريتم_أساسي، لوغاريتم_مركبي، محدد، محيط_دائرة، محيط_مربع، محيط_مستطيل، مساحة_دائرة، مساحة_مثلث، مساحة_مربع، مساحة_مستطيل، مساحة_مضلع، مسافة، مطلق، مقارنة_نسبتين، نسبة، نسبة_تراكمية، نقل، هامش_الربح، هل_يحتوي، هل_يحتوي2.
عينة (`basic_operations.ض:6-25`): `جمع/طرح/ضرب/قسمة(بقسمة آمنة: إذا(b==0) ارجع 0)/باقي/مطلق/قوة/جذر/أقصى/أدناه` — الأساس الحقيقي قبل التضخيم.

### 2.7 `networking/` — 273 تعريفًا | فريد حقيقي 69 | مولد 30

ملفات (10): `advanced_socket(10)⚠️، broadcast_protocols(10)⚠️، dns_addresses(40)، email(38)، file_transfer(40)، http_https(40)، network_sockets(10)⚠️، security_protocols(38)، tcp_udp(10)⚠️، websocket_communication(37)`.
الفريد الحقيقي (69): Curl، DELETE، FTP_تحميل، FTP_رفع، GET، HTTP_تحميل، Headers، Nameserver، POST، PUT، Rsync، SCP_نسخ، SFTP_تحميل، SFTP_رفع، TFTP_تحميل، Wget، mx_البريد، nslookup، ping، ping_WebSocket، أرسل_SSL، أرسل_WebSocket، أرسل_بريد، أرسل_بمرفقات، أرسل_ثنائي، إحصائيات_WebSocket، إنشاء_SSL، إنشاء_TLS، إنشاء_WebSocket، اتصال_SSL، اتصال_WebSocket، استبدال_نمط، استقبال_IMAP، استقبال_POP3، استقبل_SSL، استقبل_WebSocket، استقبل_البريد، اغلق_SSL، اغلق_WebSocket، افتح_لقراءة، افتح_للكتابة، بحث_موضع، تحقق_من_الاتصال، تحقق_من_البريد، تحقق_من_الشهادة، تحميل_ملف، تشفير_الاتصال، جواب_HTTP، جودة_HTTPS، حالة_WebSocket، حالة_الأمان، حالة_الخادم، حدث_WebSocket، حذف_رسالة، حل_اسم، سجل_سطر، سرعة_الاتصال، طلب_http، طول2، طول_نص، عكس_الاسم، عناوين_المضيف، عنوان_lokal، فتح_ملف، قائمة_الشهادات، قراءة_رسالة، قوالب_البريد، هل_يحتوي، هل_يحتوي2.

### 2.8 `text/` — 152 تعريفًا | فريد حقيقي 30 | مولد 26

ملفات (10): `basic_texts(10)⚠️، binding_merging(10)⚠️، encryption_encoding(36)، formatting_conversion(10)⚠️، morphological(10)⚠️، parsing_splitting(36)، search_replace(10)⚠️، tokenization(10)⚠️، translation_arabization(10)⚠️، validation_integrity(10)⚠️` — **7/10 ملفات حشو مكررة!**
الفريد الحقيقي (30): استبدال_نمط، استخرج_أرقام، استخرج_إشارات، استخرج_بريد، استخرج_روابط، استخرج_مطابقات، افتح_لقراءة، افتح_للكتابة، بحث_موضع، تشفير_64، تشفير_aes، تشفير_md5، تشفير_sha1، تشفير_sha256، تشفير_بسيط، توليد_مفتاح، حلل_csv، حلل_json، حلل_نمط، سجل_سطر، طول2، طول_نص، فتح_ملف، فك_تشفير_64، فك_تشفير_aes، فك_تشفير_بسيط، قسّم_السطور، قسّم_الكلمات، هل_يحتوي، هل_يحتوي2.

### 2.9 `time/` — 209 تعريفًا | فريد حقيقي 50 | مولد 29

ملفات (10): `calendar_dates(39)، duration_intervals(10)⚠️، home_clocks(36)، operations_system(10)⚠️، scheduling_tasks(10)⚠️، system_information(10)⚠️، time_formatting(36)، time_timestamps(10)⚠️، time_zones(38)، timer_reminders(10)⚠️`.
الفريد الحقيقي (50): UTC_الآن، UTC_لـالطابع، أيام_الأسبوع، أيام_بين، إحصائيات_الساعة، إعادة_ضبط، إعادة_ضبط_المنطقة، إيقاف_الساعة، استبدال_نمط، اسم_الشهر، اسم_اليوم، افتح_لقراءة، افتح_للكتابة، التقويم_الشمس، التقويم_الميلادي، التقويم_الهجري، المناطق_المتوفرة، المنطقة_الزمانية، الوقت_العالمي، بحث_موضع، بدء_الساعة، تاريخ_الأسبوع، تاريخ_صحيح، تثبيت_الساعة، تحليل_التاريخ، تحويل_التقويم، تحويل_المنطقة، تنسيق_ISO، تهيئة_ساعة، توقيت_الصيفي، دقائق_مضت، ساعات_مضت، ساعة_منزلية، سجل_سطر، سنة_بين، شهر_بين، طول2، طول_نص، عدد_أيام_الشهر، فتح_ملف، فرق_المنطقة، في_نطاق، منطقة_صحيحة، نبض، نسّق_التاريخ، نسّق_المدة، نسّق_الوقت، نسّق_الوقت_والثانية، هل_يحتوي، هل_يحتوي2.

### 2.10 `web/` — 364 تعريفًا | فريد حقيقي 97 | مولد 28

ملفات (10): `ajax_requests(37)، components_architecture(36)، css_styling(36)، dom_elements(38)، events_interactivity(35)، forms_input(35)، routing_navigation(37)، security_auth(37)، state_management(37)، ui_templates(36)`.
الفريد الحقيقي (97): XMLHttpRequest، _css، appendChild، createElement، fetch، getAttribute، getElementsByClassName، getElementsByTagName، has_فئة، onreadystatechange، open_request، post_data، querySelector، querySelectorAll، removeChild، response_request، send_request، setAttribute، status_request، toggle_فئة، أرسل_JSON، إحصائيات_التوجيه، إخفاء_الخطأ، إدراج_قالب، إدراج_مكون، إرسال_النموذج، إضافة_guard، إضافة_تحقق، إضافة_تصريح، إضافة_حدث، إضافة_عنصر، إضافة_فئة، إضافة_مراقب، إضافة_مسار، إضافة_مُرسل، إضافة_ورقة_أنماط، إطلاق_حدث، إعادة_تعيين، إعادة_تعيين_الأنماط، إلغاء_افتراضي، إلغاء_الرمز، إلغاء_الطلب، إنشاء_جدول، إنشاء_حالة، إنشاء_حدث، إنشاء_رمز، إنشاء_قائمة، إنشاء_قالب، إنشاء_مكون، إنشاء_موجه، إيقاف_انتشار، استيراد_الحالة، المسار_الحالي، بحث_بالمعرف، تأمين_الرأس، تتبع_التوجيه، تتبع_الحالة، تجديد_الرمز، تجميع_المكون، تحديث_المكون، تحديث_قالب، تحقق_من_الرمز، تحقق_من_المصادقة، تحقق_من_النموذج، تشفير_المدخلات، تصدير_الحالة، تطبيق_المظهر، تعيين_نمط، توجيه، حالة_المكون، حالة_النموذج، حدث_المكون، حدث_تحميل، حدث_تمرير، حدث_لوحة، حدث_نقر، حذف_المكون، حذف_حدث، حذف_فئة، حذف_قالب، حذف_مراقب، حذف_ورقة_أنماط، رجوع، رسالة_خطأ، صيغ_القالب، عرض_القالب، عيّن_الحالة، عيّن_قيمة، قائمة_التصريح، قائمة_المكونات، قيمة_الحالة، قيمة_النموذج، مسار_سابق، مسح_المسار، ملخص_الحالة، نسخ_المكون، نسخ_قالب.

## 3. التقاطع C++ ∩ JS (~30 دالة — تحفظ ولا تُكسر)

```
طول، استخرج، ابحث، استبدل، صغير، كبير، نظف، يبدأ_بـ، ينتهي_بـ، قسّم،
قوة، مطلق، أقصى، أدناه، عشوائي، مضروب، جذر، طول_مصفوفة، أضف، احذف_من،
عكس_مصفوفة، ابحث_في، افتح، سجّل، اقرأ_سطر، فصل، ليس_نهاية، أغلق، يحتوي، اقتطع،
جمع، طرح، ضرب، قسمة، باقي
```
**تحذير الانحراف (FACT):** نفس الاسم ≠ نفس التوقيع. أمثلة: `طول(string|vector)` مُحمّلة في C++ (`DaadStdlib.hpp:40,144`) مقابل نصية فقط في JS + `→.length` في المولد؛ `افتح` تُرجع `long long` مقبضًا في C++ (`Ext:13`) مقابل محاكاة تخزين في JS. اختبر كل واحدة قبل الادعاء.

## 4. دوال JS فقط (264 تعريفًا — 15 فئة فعالة، `الأمان` الأولى ميتة بالتجاوز)

> **FACT هيكلي:** `dhad-libraries.js` مفاتيحه 16 (`الأمان` مرتين `:724` و`:877` — الثانية تفوز runtime). `getCategory:2738-2754` تخدم 15. كل `code:` نص `.ض` **بجسم JS-صالح** (مثل `s.length` و`s.split` — ليست C++!). العد أدناه `defs` (تعريفات `دالة name(`) لا تكرارات المعاملات.

- **النصوص** (`:12-76`, 14): طول، استخرج، ابحث، استبدل، صغير، كبير، نظف، يبدأ_بـ، ينتهي_بـ، قسّم، يحتوي، عكس، كرر، حرف_في.
- **الرياضيات** (`:79-169`, 19): جمع، طرح، ضرب، قسمة، باقي، مطلق، قوة، جذر، أقصى، أدناه، عشوائي، عشوائي_بين، مضروب، لog⚠️(اسم هجين!)، أس_الطبيعي، sin، cos، tan، π.
- **المصفوفات** (`:172-286`, 23): إنشاء_مصفوفة، أضف، احذف_من، ابحث_في، فرز، عكس_مصفوفة، مسطح، تصفية، تحويل، مجموع، يوجد، يوجد_الكل، طول_مصفوفة، إنشاء_مكدس، ادفع، أخرج، أعلى، حجم_مكدس، إنشاء_طابور، أدخل، اسحب، مقدمة، حجم_طابور.
- **الرسوميات** (`:289-368`, 11): إنشاء_لوحة، ملء، مستطيل، دائرة، خط، نص_على_لوحة، مسح، لون، لون_عشري، خلط، تدرج.
- **الشبكات** (`:371-414`, 6): GET، POST، PUT، DELETE، تحميل_ملف، تحميل_JSON.
- **التخزين** (`:417-486`, 10): احفظ، اقرأ، احذف_تخزين، امسح_الكل، existe⚠️(إسبانية!)، قائمة_المفاتيح، إنشاء_تخزين، أضف_للتخزين، احصل_من_التخزين، احذف_من_التخزين.
- **الوقت** (`:489-555`, 14): الوقت_الحالي، الطابع_الزمني، السنة، الشهر، اليوم، الساعة، الدقيقة، الثانية، يوم_الاسبوع، اسم_الشهر، فرق، تأخير، جدولة، تكرار.
- **الملفات (محاكاة)** (`:558-607`, 8): اقرأ_ملف، اكتب_لملف، نسخ_ملف، احذف_ملف، existe_ملف⚠️، حجم_ملف، ضغط، استخرج.
- **الأحداث** (`:610-650`, 8): استمع، أوقف، أرسل_حدث، نقر، ضغط_لوحة، تمرير، تحميل، تغيير_حجم.
- **الواجهة** (`:653-721`, 13): احصل_على، احصل_على_كل، انشئ، ادرج، احذف_من، نص_العنصر، html_العنصر⚠️، صنف_العنصر، أضف_صنف، احذف_صنف، بدل_صنف، نمط، مواقع.
- **الأمان #1** (`:724-781`, 8 — **ميتة runtime بالتجاوز!**): تشفير_base64، فك_base64، تشفير_sha256، إنشاء_رمـز⚠️(شرطة غريبة!)، عشوائي_أمان، تحقق_بريد، تحقق_رابط، تحقق_رقم.
- **الاختبارات** (`:784-871`, 4): وصف، it⚠️(إنجليزية!)، توقع، تقرير.
- **الأمان #2** (`:877-1111`, 12 — الفعالة): الهاش، الهاش_MD5، hmac⚠️(إنجليزية!)، تشفير_قيصر، فك_قيصر، تشفير_xor، تشفير_AES، فك_AES، ولّد_مفتاح، ولّد_ملح، تجزئة، تحقق_كلمة.
- **الويب** (`:1116-1291`, 21): صفحة، أغلق_صفحة، عنوان1، عنوان2، عنوان3، فقرة، نص_عريض، نص_مائل، كود، رابط، صورة، قائمة، قائمة_مرتبة، جدول، جدول_عناوين، نموذج، حاوية، بطاقة، تنسيق، كلاس⚠️، اعرض.
- **الأدوات** (`:1296-2222`, 56): جمع_معلومات، فحص_منافذ، كشف_XSS، تنقية، كشف_SQLi، عدّل_SQL، كشف_Path، حلل_رؤوس، تقرير_أمان، ولّد_مفاتيح_RSA، تشفير_RSA، فك_RSA، فحص_TLS، فحص_الشهادة، كشف_Command، كشف_CSRF، ولّد_رمز_CSRF، فحص_كلمة_مرور، قواعد_جدار_النار، أضف_قاعدة، احذف_قاعدة، كشف_الاختراق، محاكاة_VPN، فحص_WiFi، فحص_DNS، كشف_DNS_مشبوه، حلل_بروتوكول، مراقبة_الشبكة، التقاط_حزم، استخراج_بيانات، استرجاع_محذوفات، تحليل_الذاكرة، كشف_عمليات_مشبوهة، تحليل_سجلات، بحث_في_سجلات، حلل_بريد، فحص_مرفقات، حلل_برمجيات، فحص_بصمة، حلل_سجلات_الويب، كشف_web_shell، تقرير_اختبار، تحقق_أخلاقي، صفحة كاملة، ترويسة، تذييل، شبكة، شريط_تنقل، زر، حقل_إدخال، تخطيط_مرن، تخطيط_شبكة، تخطيط_جانبي، بطاقة_متحركة، عمود_مرن، مركز، فاصل.
- **المحرك_الرسومي** (`:2224-2708`, 37 + 1 متداخلة `حلقة`): محرك_جديد، مسح_الشاشة، ارسم_مستطيل، ارسم_دائرة، ارسم_خط، ارسم_نص، ارسم_مستطيل_مفرغ، ارسم_دائرة_مفرغة، مفتاح_مضغوط، احصل_على_مفتاح، نقر_في_نطاق، فأرة_في_نطاق، نقر_في_دائرة، اضغط_زر، ابدأ_التحريك، حلقة (متداخلة `:2461`)، أوقف_التحريك، عرض_الشاشة، ارتفاع_الشاشة، لون_الخلفية، ارسم_صورة، حمّل_صورة، ارسم_منحنى، ارسم_متعدد، لون_عشري، لون_بـ_ألفا، لون_عشري⚠️(مكرر الاسم! `:2573` بعد `:354`؟ — INFERENCE يحتاج فحصًا)، تصادف_بين، المسافة، يتداخل، __reset_inputs⚠️، حقل_إدخال، حقل_أرقام، فعّل_حقل، تعطّل_الحقول، ضعّ_نص_حقل، احصل_على_نص_حقل، امسح_حقل.

## 5. شيم Runtime المحقونة (JS — FACT `dhad-codegen.js:86-223`)

```
__print/__array + افتح، سجّل، اقرأ_سطر، فصل، ليس_نهاية، أغلق، أضف، طول، يحتوي، اقتطع، استبدل (:101-127)
+ __dhad canvas engine (:128-223) بتسع صيغ stub (SUPPORTED_FORMATS مختصرة 9 مقابل 22 في dhad-images.js)
```

## 6. قواعد الاستخدام المعتمدة (مشتقة من R-04 + §1.4)

1. **التقاطع (~30) مقدس:** لا تغيّر اسمًا أو توقيعًا فيه إلا في الطرفين معًا.
2. **الجديد يُوسم:** أي دالة جديدة تُوسم `(C++-only)` أو `(JS-only)` حتى تُنفذ في الآخر.
3. **الأسماء الأجنبية/الهجينة** (`sin/cos/tan/π/existe/it/hmac/html/clas/لog`) **ممنوعة في الجديد** — تُهجَّن للعربية عند النقل (جيب/جيب_تمام...).
4. **المكررات** (`الأمان` المزدوجة، `لون_عشري` المزدوج، ملفات الحشو العشرية) **تُدمج أولًا** قبل أي إضافة (انظر HIGH_LEVEL_GAPS.md §10).
5. **المولدة** (`الحد_*/الفارق_*`) ليست ميزات — تُحذف من أي عدّ أو توثيق مستقبلي.
6. **الإضافة** حسب قالب HIGH_LEVEL_GAPS.md §7.5 (C++: `.ض` + اختياري `.hpp` + `using` + مرآة `stdlib_arabic` + اختبار g++؛ JS: فئة + `code:` + شيم + نسخ المرايا الثلاث + `node --check`).

## 7. UNKNOWN المعلن

- تطابق `stdlib_arabic/` سطرًا بسطر مع `stdlib/` (100 مقابل 100 ملفًا — لم تُقارن محتوياتها هنا).
- تنفيذ `DaadGraphics` (Canvas/ANSI) مقابل `dhad-images.js` سلوكيًا (واجهتان مختلفتان جذريًا — تحتاج parity test).
- دلالة دوال `.ض` ذات معاملات `دالة` (مثل `إنشاء_خيط(دالة نص)`) في التوليد C++.
- سلوك stubs الـ200 (`return 0`) عند الاستدعاء الفعلي (تُترجم لكن تُعطي صفرًا صامتًا — خطر موثق).
