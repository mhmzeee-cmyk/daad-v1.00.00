# PHASE45_CPU_REGRESSION.md — unmodified sources, full re-run (/tmp copy, +x)

`run_tests.sh`: TOTAL 96, PASS 96, FAIL 0 (T1–T16 + 8 BUG regressions, incl. nested-.if, SHL-carry).
GUI offscreen survival re-verified in Phase 3 (binary unchanged). vfat exec limitation unchanged (ENVIRONMENT).
Verdict: CPU WORKS, no regression possible (zero CPU files touched).
