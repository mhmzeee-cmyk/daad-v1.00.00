# ═══ DHAD Assembly — Generated by DAAD Compiler ═══

_start:
  load s7, 0
  call الرئيسية
  halt


# Generated by DAAD Compiler

الرئيسية:
  # ── prologue (frame_size=8) ──
  load s6, 224
  mov acc, s7
  stri s7, s6, acc
  load acc, 8
  add s7
  mov s7, acc
  load s0, 0
  load s0, 0
  load s1, 0
  load s1, 1
  jmp الرئيسية_while_cond
الرئيسية_while_cond:
  mov s2, s1
  mov acc, s2
  load s4, 5
  cmp s4
  jz الرئيسية___cmp_true_4
  jc الرئيسية___cmp_true_4
  load s3, 0
  jmp الرئيسية___cmp_done_4
الرئيسية___cmp_true_4:
  load s3, 1
الرئيسية___cmp_done_4:
  mov acc, s3
  or acc
  mov acc, s3
  jnz الرئيسية_while_body
  jmp الرئيسية_while_exit
الرئيسية_while_exit:
  mov s3, s0
  mov acc, s3
  push
  load s6, 224
  load acc, 248
  add s7
  ldri s7, s6, acc
  pop
  ret
الرئيسية_while_body:
  mov s4, s0
  mov s5, s1
  mov acc, s4
  add s5
  mov s4, acc
  mov s0, s4
  mov s5, s1
  # spill s3 -> frame[1] (rel=-7, FP-relative)
  load s6, 224
  load acc, 249
  add s7
  stri s3, s6, acc
  mov acc, s5
  load s3, 1
  add s3
  mov s3, acc
  mov s1, s3
  jmp الرئيسية_while_cond
  # ── epilogue ──
  push
  load s6, 224
  load acc, 248
  add s7
  ldri s7, s6, acc
  pop
  ret
  # frame: 8 bytes total, 1 spill slots in this func

