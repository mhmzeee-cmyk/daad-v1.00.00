# ═══ DHAD Assembly — Generated by DAAD Compiler ═══

_start:
  load s7, 0
  call الرئيسية
  halt


# Generated by DAAD Compiler

الرئيسية:
  # ── prologue (frame_size=7) ──
  load s6, 224
  mov acc, s7
  stri s7, s6, acc
  load acc, 7
  add s7
  mov s7, acc
  load s0, 0
  load s0, 20
  mov s1, s0
  mov acc, s1
  load s4, 18
  cmp s4
  jc الرئيسية___cmp_false_3
  load s2, 1
  jmp الرئيسية___cmp_done_3
الرئيسية___cmp_false_3:
  load s2, 0
  jmp الرئيسية___cmp_done_3
الرئيسية___cmp_done_3:
  mov acc, s2
  or acc
  mov acc, s2
  jnz الرئيسية_if_then
  jmp الرئيسية_if_merge
الرئيسية_if_merge:
  load acc, 0
  push
  load s6, 224
  load acc, 249
  add s7
  ldri s7, s6, acc
  pop
  ret
الرئيسية_if_then:
  load acc, 1
  push
  load s6, 224
  load acc, 249
  add s7
  ldri s7, s6, acc
  pop
  ret
  # ── epilogue ──
  push
  load s6, 224
  load acc, 249
  add s7
  ldri s7, s6, acc
  pop
  ret

