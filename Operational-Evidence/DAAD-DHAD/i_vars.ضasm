# ═══ DHAD Assembly — Generated by DAAD Compiler ═══

_start:
  load s7, 0
  call الرئيسية
  halt


# Generated by DAAD Compiler

الرئيسية:
  # ── prologue (frame_size=7) ──
  load s6, 224
  mov acc, s7
  stri s7, s6, acc
  load acc, 7
  add s7
  mov s7, acc
  load s0, 0
  load s0, 10
  load s1, 0
  load s1, 20
  mov s2, s0
  mov s3, s1
  mov acc, s2
  add s3
  mov s4, acc
  mov acc, s4
  push
  load s6, 224
  load acc, 249
  add s7
  ldri s7, s6, acc
  pop
  ret
  # ── epilogue ──
  push
  load s6, 224
  load acc, 249
  add s7
  ldri s7, s6, acc
  pop
  ret

