# ═══ DHAD Assembly — Generated by DAAD Compiler ═══

_start:
  load s7, 0
  call الرئيسية
  halt


# Generated by DAAD Compiler

جمع:
  # ── prologue (frame_size=7) ──
  load s6, 224
  mov acc, s7
  stri s7, s6, acc
  load acc, 7
  add s7
  mov s7, acc
  load s2, 0
  mov s2, s0
  load s3, 0
  mov s3, s1
  mov s4, s2
  mov s5, s3
  mov acc, s4
  add s5
  mov s0, acc
  mov acc, s0
  push
  load s6, 224
  load acc, 249
  add s7
  ldri s7, s6, acc
  pop
  ret
  # ── epilogue ──
  push
  load s6, 224
  load acc, 249
  add s7
  ldri s7, s6, acc
  pop
  ret


الرئيسية:
  # ── prologue (frame_size=7) ──
  load s6, 224
  mov acc, s7
  stri s7, s6, acc
  load acc, 7
  add s7
  mov s7, acc
  # save s1 to caller-save slot (rel=-5)
  load s6, 224
  load acc, 251
  add s7
  stri s1, s6, acc
  # save s2 to caller-save slot (rel=-4)
  load s6, 224
  load acc, 252
  add s7
  stri s2, s6, acc
  # save s3 to caller-save slot (rel=-3)
  load s6, 224
  load acc, 253
  add s7
  stri s3, s6, acc
  # save s4 to caller-save slot (rel=-2)
  load s6, 224
  load acc, 254
  add s7
  stri s4, s6, acc
  # save s5 to caller-save slot (rel=-1)
  load s6, 224
  load acc, 255
  add s7
  stri s5, s6, acc
  mov acc, s7
  push
  load s0, 20
  load s1, 22
  call جمع
  mov s6, acc
  pop
  mov s7, acc
  mov acc, s6
  push
  # restore s1 from caller-save slot (rel=-5)
  load s6, 224
  load acc, 251
  add s7
  ldri s1, s6, acc
  # restore s2 from caller-save slot (rel=-4)
  load s6, 224
  load acc, 252
  add s7
  ldri s2, s6, acc
  # restore s3 from caller-save slot (rel=-3)
  load s6, 224
  load acc, 253
  add s7
  ldri s3, s6, acc
  # restore s4 from caller-save slot (rel=-2)
  load s6, 224
  load acc, 254
  add s7
  ldri s4, s6, acc
  # restore s5 from caller-save slot (rel=-1)
  load s6, 224
  load acc, 255
  add s7
  ldri s5, s6, acc
  pop
  mov s0, acc
  mov acc, s0
  push
  load s6, 224
  load acc, 249
  add s7
  ldri s7, s6, acc
  pop
  ret
  # ── epilogue ──
  push
  load s6, 224
  load acc, 249
  add s7
  ldri s7, s6, acc
  pop
  ret

