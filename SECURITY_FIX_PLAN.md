# خطة إصلاح الثغرات الأمنية — مرتبة حسب الأولوية (توثيق فقط، لم يُنفَّذ شيء)

> **التاريخ:** 2026-09-11 — **النطاق:** `Studio/desktop-app/server/` + مرآتها `Studio/Full/server/` (متطابقتان).
> **الحالة:** قراءة وتوثيق فقط. لم يُعدَّل أي ملف كود في هذه المهمة.
> **السياق:** بعد إلغاء نظامي المعلم والمدير (المؤرشف في `/tmp/opencode/teacher-admin-removal/`) أصبح النظام **طالبًا فقط**.
> أرقام الأسطر أدناه للحالة الحالية (بعد تنظيف المسارات؛ `authController.js` الآن 706 سطرًا).
> مقياس الخطورة: 🔴 حرجة (استغلال مباشر/تعطيل) → 🟠 عالية (سرقة جلسات/تصعيد) → 🟡 متوسطة (تجاوز حدود/فقدان بيانات) → ⚪ منخفضة (تصليب).

## ملخص عددي وترتيب التنفيذ المقترح

| الأولوية | البنود | المعرفات | الحالة بعد حذف المعلم/المدير |
|---|---|---|---|
| **Phase 0 — سلامة وبيانات** | 2 | R1, GAP-F1 (جديدان، خاصان بنظام الطالب فقط) | مفتوحان — يجب حسمهما أولًا |
| **Phase 1 — حرجة** | 3 | C1, C2, C3 | مفتوحة (تمس الطالب مباشرة) |
| **Phase 2 — عالية** | 8 | H1, H2, H3, H4, H5, H6, H7, H8 | H4/H5/H6 أُغلقت وظيفيًا بالحذف (تبقى إزالة الكود الميت) — الباقي مفتوح |
| **Phase 3 — متوسطة** | 9 | M1–M9 | M6 أصبحت موضع نقاش (كانت تخص المعلم) — الباقي مفتوح |
| **Phase 4 — منخفضة** | 5 | L1–L5 | مفتوحة |
| **المجموع** | **27 بندًا** | — | **22 مفتوحًا + 3 مغلقًا وظيفيًا + 2 جديدًا** |

**القاعدة الذهبية للتنفيذ لاحقًا:** أي إصلاح يُطبَّق على `desktop-app/server` يجب تطبيقه حرفيًا على `Full/server` (أو توحيدهما) — الشجرتان متطابقتان عدا `*.bak` المحذوفة.

---

# Phase 0 — بنود نظام الطالب فقط (تُحسم قبل كل شيء)

## R1 — بقايا أدوار TEACHER/ADMIN في قاعدة البيانات + دخولهم ما زال ممكنًا 🟠 عالية

- **الملفات:** `src/controllers/authController.js:67-208 (login)` — الدخول لا يفحص الدور إطلاقًا؛ `prisma/schema.prisma:47 (role String)` — يقبل أي نص.
- **الحالة:** مفتوح. حذفنا المسارات والواجهات، لكن أي صف `User` بدور `TEACHER/ADMIN` في `dev.db` ما زال يستطيع `POST /auth/login` والحصول على JWT صالح (يُرفض فقط من مسارات `requireStudent`).
- **الوصف:** النظام "طالب فقط" على مستوى الكود، لكن البيانات القديمة تحمل أدوارًا ميتة، والدخول لا يرفضها.
- **الأثر:** حسابات معلم/مدير قديمة تبقى صالحة؛ أي مسار مشترك مستقبلي قد يمنحها امتيازًا بالخطأ.
- **الإصلاح التفصيلي:**
  1. في `login` بعد جلب المستخدم (`authController.js:106-116`) أضف:
     ```js
     if (user.role !== 'STUDENT') {
       await recordFailedAttempt(username);
       return res.status(403).json({ error: 'مرفوض', message: 'هذا النظام للطلاب فقط' });
     }
     ```
     (نفس الفحص في `studentLogin` بـ `studentManagementController.js:362-544` إن كان يقبل أدوارًا).
  2. هجرة بيانات (مرة واحدة، بعد نسخ `dev.db` احتياطيًا):
     ```sql
     -- بعد مراجعة يدوية للصفوف:
     DELETE FROM User WHERE role IN ('TEACHER','ADMIN');
     -- أو تحويلها إن لزم: UPDATE User SET role='STUDENT' WHERE role IN ('TEACHER','ADMIN');
     ```
     ثم تنظيف الصفوف اليتيمة المرتبطة (`Classroom.teacherId`, `Invitation`, `Assessment` — حسب القرار في GAP-F1).
  3. طبّق على الشجرتين.
- **التحقق:** `POST /auth/login` بحساب معلم قديم → `403`؛ `GET /student/profile` بتوكنه (إن وُجد) → `401/403`؛ عدّ `SELECT role,COUNT(*) FROM User GROUP BY role` → `STUDENT` فقط.
- **الجهد:** صغير (ساعة).

## GAP-F1 — لا أحد يستطيع إنشاء فصل/تقييم/تحدٍّ بعد حذف المعلم 🟠 عالية (فجوة وظيفية)

- **الملفات:** `src/routes/analytics.js:31-33` (حُذف `POST /classrooms`)؛ `src/routes/student.js:28` (`POST /student/join-classroom` ما زال يتطلب `inviteCode` موجودًا)؛ `assessmentController.js:186-352` (بدء/تقديم التقييم يتطلب تقييمًا موجودًا)؛ `challengeController.js:221` (العرض فقط).
- **الحالة:** مفتوح — نتيجة مباشرة للحذف ويجب حسمها بقرار تصميمي قبل إصلاح أي شيء آخر.
- **الوصف:** الطالب الجديد لا يملك أي فصل للانضمام إليه (لا إنشاء ذاتي)، ولا تقييمات للبدء بها، والتحديات للعرض فقط — المحتوى لا يأتي إلا من الـ seeds.
- **الخيارات (اختر واحدًا قبل التنفيذ):**
  - **(أ) بذرة افتراضية:** مدرسة واحدة + فصل عام `inviteCode` معلن + تحديات منشورة — أبسط، يناسب التعلم الذاتي.
  - **(ب) إنشاء ذاتي للطالب:** `POST /student/classrooms` ينشئ فصلًا يملكه الطالب لنفسه (يحوّل `Classroom.teacherId` إلى `ownerId` دلاليًا) — يحتاج تعديل `analyticsController.createClassroom:833-911` (إزالة فحص الدور + `teacherId = req.user.id`).
  - **(ج) إهمال الفصول والتقييمات:** حذف `join-classroom/my-classroom` ومسارات التقييم كلها والاكتفاء بالتحديات + خارطة الطريق — أنظف لكنه يحذف ميزات عاملة.
- **الإصلاح التفصيلي (عند اختيار أ):**
  1. سكربت `prisma/seed-student-default.js` جديد: مدرسة `مدرسة ض الافتراضية` + فصل `الفصل العام (inviteCode: DHO-START)` + نسخ تحديات `seed.js` إليها (`schoolId` المدرسة).
  2. توثيق `inviteCode` في `login.html` (قسم الطالب) كنص مساعدة.
  3. اختبار: تسجيل طالب جديد → انضمام بالكود → `GET /student/my-classroom` يعرض الفصل.
- **التحقق:** سيناريو نهاية-لنهاية لطالب جديد من التسجيل حتى أول تقديم تحدٍّ.
- **الجهد:** صغير (أ) / متوسط (ب) / متوسط (ج).

---

# Phase 1 — ثغرات حرجة 🔴

## C1 — `refresh` يفشل دائمًا: `isRefreshTokenBlacklisted` بلا `await` 🔴 حرجة

- **الملفات:** `src/controllers/authController.js:43-46` (التعريف) + `:574` (الاستدعاء) — في الشجرتين.
- **الحالة:** مفتوحة (تمس الطالب مباشرة).
- **الوصف:** الدالة `isRefreshTokenBlacklisted` تستدعي `authService.isTokenBlacklisted` وهي `async` (تُرجع `Promise`)، لكنها نفسها ليست `async` ولا تُنتظر في `:574`:
  ```js
  function isRefreshTokenBlacklisted(token) {   // :43 — ليست async
    const hash = authService.hashToken(token);
    return authService.isTokenBlacklisted(hash); // Promise — truthy دائمًا
  }
  ...
  if (isRefreshTokenBlacklisted(token)) {        // :574 — بلا await
  ```
  كائن `Promise` truthy دائمًا → كل `POST /auth/refresh` ينتهي `401 + مسح الكوكيز` حتى للتوكن السليم.
- **الأثر:** DoS وظيفي لتدوير الجلسات — كل طالب يُجبر على إعادة الدخول كل ساعة (عمر `access_token`)؛ والواجهة (`api.js:222-248`) ستدخل حلقة تحويل لصفحة الدخول.
- **الإصلاح التفصيلي:**
  1. اجعل الدالة `async` وأضف `await` في الاستدعاء:
     ```js
     async function isRefreshTokenBlacklisted(token) {
       const hash = authService.hashToken(token);
       return authService.isTokenBlacklisted(hash);
     }
     ...
     if (await isRefreshTokenBlacklisted(token)) {
     ```
  2. بديل أصلب (يُستحسن): احذف الغلاف واستدعِ الخدمة مباشرة:
     ```js
     const hash = authService.hashToken(token);
     if (await authService.isTokenBlacklisted(hash)) {
     ```
  3. أضف اختبار `tests/integration/auth.refresh.test.js`: دخول → `refresh` بالكوكي → `200`؛ ثم `refresh` بالتوكن القديم (المُدوَّر) → `401`؛ ثم `refresh` بلا توكن → `400`.
  4. طبّق على الشجرتين حرفيًا.
- **التحقق:** الاختبار الجديد أخضر + `POST /auth/refresh` يدويًا مرتين (الثانية بالقديم تفشل).
- **الجهد:** دقائق (أعلى عائد).

## C2 — صندوق `vm` ليس حدًا أمنيًا والحدود المعلنة غير مطبقة 🔴 حرجة

- **الملفات:** `src/utils/dhadSandbox.js:10-18` (الحدود) + `:26-28` (الوحيد المطبق: طول الكود) + `:47-84` (الـ Proxy) + `:97-178` (الكائنات المكشوفة) + `:189-212` (التنفيذ) — في الشجرتين.
- **الحالة:** مفتوحة (قلب تقييم الطالب).
- **الوصف:** الملف يعلن 7 حدود (`TIMEOUT 5s`, `64KB` مخرجات, `50KB` كود, `32MB` heap, `100k` تكرار, `1k` عمق, `10k` متغير) لكن المطبق فعليًا حدّان فقط: طول الكود + `timeout` الـ `vm`. فحص الـ heap (`:220` يبحث عن كلمة `heap` في الرسالة) لا يُرمى أبدًا من `vm`. والأخطر: `vm` في Node **ليست عزلًا أمنيًا** — كائنات المضيف (`Object, Array, RegExp, Date, Error, Math...`) تُمرَّر **بالمرجع** (`:97-178`)، والـ Proxy يحجب الوصول المباشر لـ `constructor/__proto__/prototype` فقط، بينما تبقى مسارات معروفة (`Object.getPrototypeOf`, `Symbol`, `Promise/AsyncFunction`, سلاسل `Proxy/Reflect`) قد تفضي إلى `Function("return process")` → **RCE**.
- **الأثر:** طالب خبيث (أو كود تحدٍّ ملغوم يُنفَّذ server-side عند التقديم `student.js:176-365`) قد يهرب من الصندوق وينفذ أوامر على الخادم أو يستنزف الذاكرة/المعالج (لا حد تكرار/عمق/heap فعلي).
- **الإصلاح التفصيلي (على مرحلتين):**
  - **سريع (تخفيف فوري):**
    1. جمّد الكائنات المكشوفة واحذف الخطرة: مرر نسخًا مجمدة من `Math, JSON` فقط + `__print/__array/__input`، واحذف `Object, Array, String, Number, Boolean, RegExp, Date, Error` من السياق (`:97-141`) أو جمّد نماذجها (`Object.freeze` لكل prototype قبل التمرير).
    2. احجب `getPrototypeOf/getOwnPropertyNames/Reflect/Symbol/Promise` بإضافتها لـ `DANGEROUS_PROPS` (`:47-51`) وتمرير بدائل آمنة.
    3. طبّق حد المخرجات فعليًا: غلّف `__print` بعدّاد بايتات يرمي عند `64KB`؛ وحد طول الكود موجود (`:26`) أبقه.
    4. اختبار خصومة: ملف `tests/unit/dhadSandbox.escape.test.js` بعشرات محاولات الهروب المعروفة (constructor سلسلة، `getPrototypeOf`، `AsyncFunction`، حلقة لا نهائية → يجب `timeout`، recursion عميق → يجب رفض).
  - **جذري (الحل الصحيح):**
    1. انقل التنفيذ إلى عملية منفصلة (`worker_threads` + `resourceLimits: { maxOldGenerationSizeMb: 32, stackSizeMb: 2 }` + `timeout` قاتل) أو مكتبة `isolated-vm` (V8 isolate حقيقي بذاكرة محددة).
    2. أضف `CPU/memory cgroup` على مستوى الحاوية (Docker `mem_limit + cpus`) كحزام ثانٍ.
    3. أعد كتابة `serverEvaluator.js` ليعتمد نتيجة العملية المعزولة فقط.
- **التحقق:** كل اختبارات الهروب تفشل في الهروب (تُرفض/تُوقف) + اختبار حمل (كود حلقة مليار تكرار يُقتل في ≤6s دون التأثير على طلبات أخرى).
- **الجهد:** متوسط (تخفيف) / كبير (عزل حقيقي) — **أكبر بند في الخطة**.

## C3 — كلمات مرور وبيانات Seed ملتزمة في المستودع 🔴 حرجة

- **الملفات (أمثلة مؤكدة):** `prisma/seed_admin.js:35,52 (admin123)` + `prisma/seed_ui_test.js:10 (Test1234!)` + `seed-data.js:16 (Test1234!)` + `create-accounts.js:7,116-124 (Test1234! + إيميلات)` + `scripts/mega_classroom_simulator.js:14 (Test456)` — والملفات المحذوفة (`seed_admin`, `mega_simulator`) ما زالت في الأرشيف والتاريخ.
- **الحالة:** مفتوحة (الأسرار في تاريخ git حتى بعد حذف الملفات من الشجرة).
- **الوصف:** كلمات مرور حقيقية مكتوبة في الكود؛ أي تشغيل seed في staging/prod يزرع حسابات بكلمات معروفة؛ والتاريخ يحتفظ بها للأبد ما لم يُطهَّر.
- **الإصلاح التفصيلي:**
  1. استبدل كل كلمة مكتوبة بتوليد عشوائي عند التشغيل (`crypto.randomBytes(16).toString('hex')`) تُطبع مرة واحدة في stdout ولا تُحفظ.
  2. أضف فحصًا في CI (`git-secrets` أو `gitleaks`) يمنع دمج أي `password|passwd|secret.*=.*['"].{4,}`.
  3. دوّر أي بيئة استُخدمت فيها هذه الكلمات (غيّر كلماتها فورًا).
  4. (اختياري، عند نشر عام): طهّر التاريخ بـ `git-filter-repo` للملفات الحساسة — بحذر وبتنسيق مع الفريق لتعارض الـ history.
  5. طبّق على الشجرتين وأي seed جديد (بما فيه seed البند GAP-F1).
- **التحقق:** `gitleaks detect` نظيف + `grep -ri "Test1234\|admin123" --include=*.js src prisma scripts` فارغ (عدا الاختبارات الوهمية المعزولة إن وُجدت).
- **الجهد:** صغير.

---

# Phase 2 — ثغرات عالية 🟠

## H1 — مفتاح الـ refresh احتياطيًا هو مفتاح الـ access 🔴→🟠 (فصل المفاتيح)

- **الملفات:** `src/services/authService.js:72-79` (التوقيع) + `:92-96` (التحقق) + `studentManagementController.js:508` + `.env.example:30-32` — الشجرتان.
- **الحالة:** مفتوحة.
- **الوصف:** `jwt.sign(..., JWT_REFRESH_SECRET || JWT_SECRET)` — عند غياب متغير الـ refresh يُستخدم مفتاح الـ access للنوعين؛ تسرب مفتاح واحد يزوّر النوعين. فحص `type:refresh` (`authController:603`) يخفف الخلط لكنه لا يعوّض فصل المفاتيح.
- **الأثر:** اختراق مفتاح واحد = انتحال كامل (دخول + تدوير).
- **الإصلاح:**
  1. عند الإقلاع (`index.js` بجانب `ensureSecureSecrets`): إن غاب `JWT_REFRESH_SECRET` أو ساوى `JWT_SECRET` → **ارفض الإقلاع** (`throw` برسالة واضحة) بدل السقوط الاحتياطي.
  2. ولّد قيمًا متميزة 64 بايت لكل بيئة وخزّنهما في مدير أسرار (لا في المستودع).
  3. حدّث `.env.example` بتعليق "مطلوب ومستقل".
- **التحقق:** إقلاع بلا `JWT_REFRESH_SECRET` يفشل برسالة؛ توكن refresh موقع بمفتاح access يُرفض.
- **الجهد:** صغير.

## H2 — التوكن مكرر: كوكي `httpOnly` + نسخة مقروءة في الجسم 🟠

- **الملفات:** `authController.js:56-59` (كوكيز) + `:184-204` (يُرجع `accessToken` رغم تعليق `:184` "NOT returned")؛ مقابل `refresh :657-662` (كوكيز فقط) و`verifyOTP` (جسم فقط بلا كوكيز).
- **الحالة:** مفتوحة (الواجهة الحالية لا تخزن التوكن — `api.js:60-68` no-op — لكن أي XSS مستقبلي يسرق نسخة الجسم).
- **الوصف:** ازدواجية تربك العملاء (الواجهة تتوقع أشكالًا مختلفة في register/verify/refresh — سُجلت سابقًا) وتضاعف سطح التسريب.
- **الإصلاح:**
  1. اعتمد **كوكيز فقط**: احذف `accessToken` من جسم `login` (`:187-204` → أبقِ `expiresIn/userId/username/role/profile`) ووحّد `refresh` و`verifyOTP` على نفس الشكل (كوكيز + `{success, expiresIn, profile?}`).
  2. حدّث `api.js` (إزالة أي قراءة `data.accessToken` — تحقق أنها no-op فعلًا) و`cli-login.js:75` الذي يعتمد على نسخة الجسم (انقله للكوكيز أو وثّقه كاستثناء CLI).
  3. أضف `__Host-` prefix؟ (يتطلب `Secure + Path=/` وبلا `Domain` — قيّمه مع بيئة الإنتاج).
- **التحقق:** لا يظهر أي `*token*` في أجسام JSON (فحص `tests/integration` يُحدَّث) + الدخول/التحديث/الخروج يعمل بالكوكيز فقط.
- **الجهد:** صغير-متوسط.

## H3 — `clearTokenCookie` لا يمسح الكوكيز فعليًا 🟠

- **الملفات:** `authController.js:49-54` (الإعداد: `httpOnly, secure?, sameSite:strict`) مقابل `:61-64` (المسح: `path + sameSite` فقط).
- **الحالة:** مفتوحة.
- **الوصف:** المتصفح يطابق خصائص المسح مع الإنشاء؛ إسقاط `httpOnly/secure` قد يُبقي كوكيز `Secure/HttpOnly` حية بعد logout/تدوير/حظر.
- **الإصلاح:** وحّد الخصائص:
  ```js
  function clearTokenCookie(res) {
    res.clearCookie('access_token', { ...COOKIE_OPTIONS, maxAge: 0 });
    res.clearCookie('refresh_token', { ...COOKIE_OPTIONS, maxAge: 0 });
  }
  ```
  (انشر `COOKIE_OPTIONS` نفسها بدل إعادة الكتابة).
- **التحقق:** اختبار متصفح/ـ supertest: بعد `logout` لا تُرسل الكوكيز في الطلب التالي (ترويسة `Set-Cookie` منتهية بنفس الخصائص).
- **الجهد:** دقائق.

## H4 — المعلم كان يعدّل تحديات النظام `schoolId:null` 🟢 مغلّق

- **الملفات (مؤرشفة/ميتة):** `challengeController.js:458-568` (`if(existing.schoolId && …)` يتجاوز `null`) مقابل الحماية الصحيحة في الحذف `:526-531`.
- **الحالة:** **مُغلَق بالكامل** — الدوال `create/update/delete/publish/setDaily` حُذفت من `challengeController.js` (+ إزالتها من `module.exports`) بعد التأكد بـ `grep` من صفر مستوردين. اختبارات الوحدة المحيطة بالحذف (sprint4) حُدّثت لفحص غياب الكود الميت.
- **التنفيد:** 2026-09-12 — البند 4 من قائمة الإزالة.

## H5 — الـ ADMIN كان محجوبًا خطأً عن إدارة الطلاب 🟢 مغلقة وظيفيًا

- **الملفات (مؤرشفة/ميتة):** `studentManagement.js:65-83` (مسارات `requireAdmin`) + `studentManagementController.js:552-836` (فحص `teacherId` يساوي id المدير → `403` دائم).
- **الحالة:** **مغلقة وظيفيًا** — المسارات حُذفت كلها؛ لا إدارة طلاب إدارية بعد (النظام طالب فقط).
- **المتبقي:** لا شيء وظيفيًا؛ إزالة الدوال الميتة مع H4 في نفس الدفعة.
- **الجهد:** مدمج مع H4.

## H6 — الـ onboarding: OTP جماعي + `xlsx` بلا تطهير 🟢 مغلقة وظيفيًا

- **الملفات (محذوفة):** `routes/onboard.js` + `controllers/onboardController.js:91-180` (رمز واحد 6 أرقام للجميع + يُرجع في JSON + حذف مستخدمين + `XLSX.read` بلا تصليب) — كلها في الأرشيف.
- **الحالة:** **مغلقة وظيفيًا** — لا onboarding بعد. إن أُعيدت الميزة مستقبلًا للطالب الذاتي: رمز لكل مستخدم + تسليم خارج النطاق + حدود صفوف + فحص مضاد فيروسات/صيغ + تخزين قرصي بدل الذاكرة.
- **الجهد:** لا شيء الآن (قرار مستقبلي).

## H7 — `CSRF_SECRET` مشتق من `JWT_SECRET` 🟠

- **الملفات:** `middlewares/csrf.js:9-10` (`CSRF_SECRET = env.CSRF_SECRET || HMAC(JWT_SECRET||'fallback-csrf')`).
- **الحالة:** مفتوحة.
- **الوصف:** تسرب `JWT_SECRET` يزوّر CSRF؛ وإن غاب الاثنان فالقيمة static قابلة للتنبؤ.
- **الإصلاح:**
  1. اجعل `CSRF_SECRET` **مطلوبًا ومستقلًا** (32 بايت) وارفض الإقلاع بدونه (مثل H1).
  2. أضفه لمدير الأسرار و`.env.example` كمتغير إلزامي.
- **التحقق:** إقلاع بدونه يفشل؛ توكن CSRF موقع بالمفتاح القديم يُرفض بعد التدوير.
- **الجهد:** صغير (يُدمج مع H1 في "دفعة الأسرار").

## H8 — حدود `MemoryStore` تتباين بين العمال (بلا Redis) 🟠

- **الملفات:** `cluster.js:10` (عمال بعدد الأنوية) + `nginx.conf:6-8` (`least_conn` لا `ip_hash`) + `strictSecurity.js:16-145` (خرائط ذاكرة) + `security.js/router` (`express-rate-limit` الافتراضي) + `authService.js:108-136` (قائمة حظر ذاكرة) + `apiSecurity.js:21` (`nonces` بلا TTL) + `csrf.js:13` (خريطة ميتة).
- **الحالة:** مفتوحة.
- **الوصف:** بلا `REDIS_URL` كل عامل يحمل عدّاداته؛ المهاجم ينال `N×` الحد ويتجاوز القفل/الحظر بالتناوب؛ و`nonces` تتسرب حتى OOM.
- **الإصلاح:**
  1. في الإنتاج/cluster: **Redis إلزامي** (`rate-limit-redis` + مخازن القفل/الحظر/الـ nonces عليها بـ TTL) أو شغّل عاملًا واحدًا (`WEB_CONCURRENCY=1` + `ecosystem instances:1` — الموجود فعلًا لـ SQLite) ووثّق القيد.
  2. أعطِ `nonces` TTL وحَدًّا أعلى، واحذف خريطة `csrfTokens` الميتة.
  3. بدّل Nginx إلى `ip_hash` إن بقي متعدد العمال بلا Redis (تخفيف جزئي).
- **التحقق:** اختبار حدّ عبر عاملين: 31 طلبًا/دقيقة من نفس IP → `429` (لا `60`).
- **الجهد:** متوسط.

---

# Phase 3 — ثغرات متوسطة 🟡

## M1 — تجاوز CSRF ميت/مرتّب خطأً + `Lax` + صفحة منسية 🟡

- **الملفات:** `middlewares/csrf.js:64-68` (تجاوز `req.user+Bearer` — ميت لأن `csrfProtection` في `index.js:152` قبل `authenticate`)؛ `apiSecurity.js:122-156` (حماية بديلة غير مُركّبة)؛ الكوكي `lax` (`:38-43`) مقابل `strict` للمصادقة؛ `forgot-password.html` غير موجودة أصلًا (الـ fetch فيها بلا CSRF سيفشل `403`).
- **الإصلاح:** رتّب `authenticate → csrf` أو وحّد على نسخة `apiSecurity` (تجاوز Bearer بتوكن صالح)؛ اجعل كوكي المصادقة `Strict` وأبقِ كوكي CSRF `Lax`؛ وحدّد مصير `forgot-password` (صفحة تستخدم `api.request` أو إزالتها).
- **التحقق:** عميل Bearer صالح بلا CSRF يعمل؛ متصفح بلا CSRF يُرفض `403`؛ صفحة النسيان تعمل أو محذوفة.
- **الجهد:** صغير.

## M2 — `strictSanitize` يقطع كود الطالب عند 500 حرف 🟡

- **الملفات:** `strictSecurity.js:184-186,389-392` (قصّ `substring(0,500)`) + `index.js:105` (عمومي)؛ مكرر مع `security.js:5-35` + `router/index.js:24`.
- **الإصلاح:** استثنِ `code/password` من القصّ (أو ارفع حد `code` إلى `50KB` الموافق لحد الصندوق)؛ وحّد طبقة التعقيم الواحدة وأزل الأخرى.
- **التحقق:** تقديم كود 5KB يعمل ويُقيَّم كاملًا؛ كلمة مرور 72+ حرفًا لا تُقطع قبل `bcrypt` (لاحظ حد `bcrypt` الفعلي 72 بايتًا — وثّقه).
- **الجهد:** صغير.

## M3 — تنقيح الاستجابة سطحي ومفرط 🟡

- **الملفات:** `apiSecurity.js:16-104` (قائمة حساسة + نسخ سطحي `{...obj}`).
- **الإصلاح:** تنقيح عميق متكرر + قائمة سماح لكل مسار (أبقِ `email/nationalId` حيث يحتاجه العميل، واحجب `*token/passwordHash` دائمًا بما فيها المتداخلة).
- **التحقق:** اختبار تسرب: لا `password|token` في أي استجابة (بما فيها `profile.school` المتداخلة).
- **الجهد:** صغير.

## M4 — نوافذ/رسائل الـ rate والقفل متناقضة + حظر IP ميت 🟡

- **الملفات:** `strictSecurity.js:161` (نافذة 15د) مقابل رسالة "per minute" (`:223-226`)؛ `nginx (5r/m)` مقابل التطبيق (20-60)؛ `isAccountLocked:266-282` لا يفحص `delayUntil`؛ `recordIPFailure/isIPBlocked:298-329` بلا أي استدعاء.
- **الإصلاح:** وحّد النوافذ والرسائل (دقائق معلنة = دقائق فعلية)؛ فعّل فحص `delayUntil` في البوابة؛ و either اربط حظر IP بأخطاء `401` أو احذف الدوال الميتة.
- **التحقق:** سيناريو 6 محاولات فاشلة → `423` + `Retry-After` صحيح.
- **الجهد:** صغير.

## M5 — `ensureSecureSecrets` يعيد كتابة `.env` تلقائيًا 🟡

- **الملفات:** `index.js:24-56` (يستبدل صيغة مقتبسة فقط؛ `.env.example` غير مقتبس → الدوران لا يدوم؛ يُبطل كل JWTs بصمت).
- **الإصلاح:** افشل عند الإقلاع بمفتاح ضعيف (رسالة واضحة) ولا تكتب ملفات أبدًا؛ انقل التدوير لسكربت `scripts/rotate-secrets.js` يدوي.
- **التحقق:** إقلاع بمفتاح `change-me` يتوقف فورًا برسالة.
- **الجهد:** صغير.

## M6 — فروع RBAC ميتة وتوسيع غير مكتمل 🟡 (مُغلَق بالكامل)

- **الملفات:** `reportsController.js:208` (فرع `STUDENT` — الملف كله محذوف الآن ✅)؛ `auth.js:295` (`requireStudent=STUDENT` حصري — أصبح مثاليًا لنظام طالب فقط ✅)؛ `challengeController.js:732-737` (ترشيح المعلم — ميت الآن).
- **الحالة:** **مُغلَق بالكامل** — حُذف `requireAdmin/requireTeacher/requireTeacherOrAdmin` (المتبقي `requireSchoolAccess` مُحتفظ به لاستخدامه الفعلي في `routes/schools.js`) + حُذف `requireOwnership` و `getModelFromRoute` (صفر مستوردين مؤكد بـ `grep`) — تنفيذ 2026-09-12.
- **الجهد:** صغير (تم).

## M7 — كشف الغش: استثناء `tier≤2` + ثغرات تمويه + `__input→0` 🟡

- **الملفات:** `serverEvaluator.js:218-220` (تخطي الصغار) + `:225-369` (أنماط محدودة) + `:304-325` (مخرجات ديناميكية يتجاهلها الكشف)؛ `dhadSandbox.js:139-141` (`__input(){return 0}`)؛ المسار الموثوق القديم `challengeController:638-719` ميت (`410` في المسارات ✅).
- **الإصلاح:** فعّل الكشف كل المستويات؛ أضف تطبيع إزالة التمويه (concatenation/template/`fromCharCode`/base64/طباعات متعددة)؛ ومرر مدخلات `__input` الحقيقية من تعريف التحدي server-side بدل الصفر.
- **التحقق:** عينة غشاشة (قيم مطبوعة مباشرة/متخفية) تُرفض في كل المستويات؛ تحديات الإدخال تُقيَّم بمدخلاتها.
- **الجهد:** متوسط.

## M8 — CORS وخوادم الواجهة 🟡

- **الملفات:** `index.js:175-205` (مطابقة تامة، `OPTIONS` بلا `Origin` → `403`، ترويسات مخصصة ناقصة)؛ `nginx.conf:55-60` (`SAMEORIGIN + unsafe-inline/eval` يعارض `DENY` التطبيق مع `helmet CSP:false`).
- **الإصلاح:** اجعل `ALLOWED_ORIGINS` إلزاميًا في الإنتاج (بلا افتراضي `localhost`)؛ أضف الترويسات المخصصة (`X-CSRF-Token, X-Request-*, HMAC`)؛ وحّد سياسة الإطارات بين التطبيق وNginx.
- **التحقق:** أصل مرفوض → حظر نظيف؛ الأصل المسموح يعمل كاملًا (كوكيز + CSRF).
- **الجهد:** صغير.

## M9 — السجلات والصحة 🟡

- **الملفات:** `logger.js:135-136` (HMAC مقتطع ✅ جيد) + `:188` (يتجاوز `/health` فقط لا `/ready/live`) + `:192-195` (تسجيل انتقائي)؛ `logAudit:460-475` (ذاكرة 10k لا تُحفظ)؛ `securityLogger:106-170` غير مستدعى؛ `healthController.js:35` (مقارنة `===` للمفتاح).
- **الإصلاح:** تجاوز `/health/*`؛ احفظ التدقيق (ملف/جدول)؛ استخدم `securityLogger` من المتحكمات؛ وقارن المفاتيح بـ `timingSafeEqual`.
- **التحقق:** لا ضجيج صحة في السجلات + أحداث أمنية محفوظة + مفتاح صحة بطول مختلف لا يسرب زمنًا.
- **الجهد:** صغير.

---

# Phase 4 — تصليب منخفض ⚪

## L1 — نظافة الأسرار (جيدة أصلًا) ⚪

- **الحالة:** `.env` غير ملتزم (`.gitignore:2` ✅) ولا تُسجل الروابط (`redis.js:40-41` ✅) وملخص الإقلاع أطوال فقط (`index.js:361-375` ✅).
- **الإصلاح:** صحح نص خطأ `encryption.js:31` (يذكر `JWT_SECRET` كبديل غير موجود فعليًا في `:20`) + أكمل L-تدوير بذور الاختبار (مدمج مع C3).

## L2 — حراسة المسار + حدود الجسم ⚪

- **الملفات:** `index.js:134-145,210-239` (حدود 5MB/1MB + كشف `.. %2e %252e \ \0` متأخر بعد `json/cookie/CSRF`؛ `decodeURIComponent` قد يرمي `500`).
- **الإصلاح:** انقل الحارس أولًا والتقط خطأ الفك بـ `400`؛ وحّد الحدود مع `strictSecurity (1MB/5MB)`.

## L3 — صيغة CSV معطوبة شكليًا ⚪

- **الملفات:** `assessmentController.js:924-932` (`return `"'"${escaped}"`` بعلامة زائدة — آمن عمليًا ببادئة `'` لكن شكله خطأ).
- **الإصلاح:** `return "'" + escaped` داخل حقل مقتبس حسب OWASP؛ (الدالة ميتة حاليًا بعد حذف مسار التصدير — تُصلح مع دفعة إزالة الكود الميت أو تُترك).

## L4 — سباق إنشاء مزدوج (آمن بالصدفة) ⚪

- **الملفات:** `courseController.js:92-101` (فحص ثم إنشاء → TOCTOU) يُنقذه قيد التفرد `P2002→409` (`errorHandler.js:24` ✅).
- **الإصلاح:** استخدم `upsert/connectOrCreate` بدل الفحص المسبق.

## L5 — `expectedOutput` محجوبة صحيحًا (ليست ثغرة — توثيق) ⚪

- **الملفات:** `studentProfileController:622-634` + `courseController:137-147` + `challengeController:277` + `daily:297-348` — كلها تحجب المخرجات المتوقعة عن الطالب ✅. تُذكر لمنع "إصلاح" يكشفها بالخطأ مستقبلًا.

---

# ملحق أ — ترتيب التنفيذ المعتمد (عند إعطاء إشارة التنفيذ)

1. **Phase 0:** حسم GAP-F1 (قرار أ/ب/ج) → تنفيذ R1 (بوابة الدخول + هجرة الأدوار بعد نسخ `dev.db`).
2. **Phase 1:** C1 (دقائق) → C3 (تطهير + فحص CI) → C2 (تخفيف فوري ثم عزل حقيقي).
3. **Phase 2 (دفعة الأسرار):** H1+H7 (إقلاع صارم) → H3 → H2 (توحيد الأشكال + تحديث العملاء والاختبارات).
4. **Phase 2 (بنية):** H8 (Redis أو عامل واحد موثق) + إزالة الكود الميت (H4/H5/M6/L3).
5. **Phase 3:** M5 → M2 → M4 → M1 → M3 → M8 → M9 → M7.
6. **Phase 4:** L1 → L2 → L4 (+ L5 توثيق فقط).
7. **ختام كل phase:** `npm test` + سيناريو طالب نهاية-لنهاية (تسجيل → انضمام → حل → تقييم → قيادة) + تحديث هذا الملف (نقل البند لـ "مُصلح + رقم الالتزام").

# ملحق ب — مبادئ ملزمة للتنفيذ لاحقًا

- **Fail-closed:** أي سر missing = رفض إقلاع، لا قيم احتياطية.
- **فصل المفاتيح:** مفتاح لكل غرض (دخول/تدوير/CSRF/HMAC/تشفير).
- **كوكيز فقط للتوكنات** + `SameSite=Strict` للمصادقة.
- **لا ثقة بالعميل أبدًا** في التقييم (الخادم يحسب كل شيء — قائم فعلًا في `student.js:176-365` ✅).
- **المرآتان معًا دائمًا** (`desktop-app/server` + `Full/server`) أو توحيدهما أولًا.
- **اختبار لكل إصلاح** (وحدة + تكامل) قبل الانتقال للبند التالي.

> **نهاية التوثيق — بانتظار إشارة التنفيذ.** لا يُنفَّذ أي بند إلا بأمر صريح لاحق، وبندًا بندًا حسب هذا الترتيب.

---

# سجل التنفيذ — 2026-09-11 (الدفعة الأولى: Phase 0 + Phase 1 + حزمة الأسرار)

| البند | الحالة | ما نُفذ (الشجرتان ما لم يُذكر) | التحقق |
|---|---|---|---|
| R1 | ✅ مُصلح | بوابة `role !== STUDENT → 401` في `login` + حراسة أنواع البريد/الاسم/الوطني؛ هجرة `dev.db`: حذف 52 TEACHER + 5 ADMIN (نسخة `/tmp/opencode/teacher-admin-removal/dev.db.pre-R1-backup-*`) → 517 STUDENT | اختبارات `student-only` (رفض TEACHER/ADMIN + فرض الدور) ✅ |
| GAP-F1 (خيار أ) | ✅ مُصلح | سكربت `prisma/seed-student-default.js` + أمر `db:seed:student` + تحديث جداول `README`؛ شُغّل: مدرسة + مالك + فصل `DHO001` + 3 تحديات | إخراج السكربت + `DHO001` معلن |
| C1 | ✅ مُصلح | `async isRefreshTokenBlacklisted` + `await` في الفحص والحظر | اختبار دوران + رفض إعادة الاستخدام ✅ |
| C3 | ✅ مُصلح (النواة) | كلمات عشوائية (`SEED_PASSWORD`) في `seed-data` + `create-accounts` (×3 نسخ) + `seed_ui_test` + env في `test-quick/realistic-test`؛ مهمة CI `secret-scan` (gitleaks + grep) | بوابة grep محليًا ✅ + `node --check` ✅ |
| C2 (تخفيف) | ✅ مُصلح | حجب `getPrototypeOf` (كائن+دالة) + حد مخرجات صلب 64KB + اختبار `dhadSandbox.hardening` (7 حالات)؛ اكتُشف وأُصلح كسر `Array.prototype.slice` أثناء العمل | 55/55 صندوق ✅ + 95/95 حزم متأثرة ✅ |
| H1+H7+M5 | ✅ مُصلح | `assertStrongSecrets` (إقلاع صارم، بلا توليد/كتابة) + سكربت `generate-secrets.js` + توليد `.env` للبيئتين + تحديث `tests/helpers` | سلبي: خروج 1 برسالة ✅ + إيجابي: إقلاع `port 33387` ✅ |
| H3 | ✅ مُصلح | `clearTokenCookie` تعكس `COOKIE_OPTIONS` | مغطى باختبارات logout القائمة ✅ |
| H2, H8, M1–M9, L1–L5 | ✅ مُصلحة | أُنجزت في الدفعة اللاحقة — انظر سجل التنفيذ أدناه (2026-09-12) | جدول الدفعة الثانية |

**ملاحظات الدفعة:** (1) كسر `arrays` من حجب `prototype` شُخّص عبر `git stash` وأُصلح بحدّ الحجب على `constructor/getPrototypeOf` مع توثيق السبب (قيود V8) في الكود والاختبار. (2) اختباران قديمان في `auth.test.js` كانا يوثقان العقد الملغاة (تسجيل المعلم) حُدّثا للعقد الجديد. (3) `seed-data.js` ما زال يزرع صفوف TEACHER كمالكين هيكليين للفصول (لا دخول لهم بعد R1) — موثق كدين لحين قرار إزالته. (4) `test:mega` أُزيل من `package.json` (سكربته محذوف سابقًا).

**نتيجة التحقق النهائي (نفس اليوم):** كامل `tests/unit + tests/integration` أخضر **746/746** بعد تحديث 14 اختبارًا كانت توثق عقود المعلم/المدير الملغاة (`auth` ×2، `challengeRoutes` ×5، `studentRoutes` ×1، `sprint3` ×8 استبدلت بفحوص الإزالة، `sprint4/5` فحوص غياب الصفحات). لا فشل متبقٍّ.
**المتبقي للدفعات التالية:** H2 (كوكيز فقط) + H8 (Redis/عامل واحد) + M1–M9 + L1–L5 — ~~لم تُمس بعد~~ **أُنجزت بالكامل** في الدفعة الثانية (2026-09-12) — انظر سجل التنفيذ أدناه.

---

# سجل التنفيذ — 2026-09-12 (الدفعة الثانية: H2 + H8 + M1–M9 + L1/L2/L4)

| البند | الحالة | ما نُفذ (الشجرتان + المرايا الأمامية حيث يلزم) | التحقق |
|---|---|---|---|
| H2 | ✅ مُصلح | الدخول بلا `accessToken` في الجسم (كوكيز فقط) + تحديث `cli-login.js` + أمثلة `swagger` + تحديث اختبار قديم كان يوثق التسريب | حيًا: `TOKEN-IN-BODY: False` ✅ + حزم المصادقة ✅ |
| H8 | ✅ مُصلح | عامل واحد عند غياب `REDIS_URL` + حد/طرد لـ `nonces` + حذف خريطة CSRF الميتة + `nginx ip_hash` | `node --check` ✅ (لا اختبار حمل — موثق كقيد) |
| M1 | ✅ مُصلح | حذف تجاوز Bearer الميت + `forgot-password.html` تجلب CSRF أولًا (×3 مرايا) | حزم CSRF ✅ |
| M2 | ✅ مُصلح | إعفاء `code/password/*Password/starterCode` من قصّ 500 حرف (تمرير اسم الحقل) | حزم الأمان ✅ |
| M3 | ✅ مُصلح | تنقيح عميق متكرر للاستجابات | حزم الأمان ✅ |
| M4 | ✅ مُصلح | تصحيح رسالة النافذة (15 دقيقة) + ربط `recordIPFailure` بكل 401 دخول + فحص `isIPBlocked` أولًا (423) | حزم المصادقة ✅ |
| M6 | ✅ مُصلح | حذف `requireAdmin/Teacher/TeacherOrAdmin` (صفر مستوردين) | `grep` صفر ✅ |
| M7 | ✅ مُصلح | الكشف من المستوى 2 (الأول معفي: starter يحوي الحل) + دمج/قوالب/`fromCharCode`/base64 حقيقي + أوزان + 5 اختبارات | `cheatingDetection` ✅ + `security-evaluator` ✅ |
| M8 | ✅ مُصلح | `ALLOWED_ORIGINS` إلزامي إنتاجًا + ترويسات `Timestamp/Nonce` + `OPTIONS` بلا Origin → 204 | حزم التكامل ✅ |
| M9 | ✅ مُصلح | تجاوز `/health/*` + مقارنة `timingSafeEqual` (+ استيراد `crypto` الناقص) | حزم الصحة ✅ |
| L1/L2/L4 | ✅ مُصلحة | رسالة `ENCRYPTION_KEY` + `try` حول `decodeURIComponent` + سباق التسجيل عبر `P2002` | الحزم ✅ |
| L3/L5 | ➖ لا عمل | L3 ميتة مع ملفها المحذوف؛ L5 توثيقية أصلًا | — |
| صيانة اختبارات | ✅ | تحديث 14+ عقدة قديمة أخرى (4 `sprint` + `phase2` + تحويل فحوص الإزالة) | **المجموعة الكاملة: 716/716** ✅ |
| الواجهة | ✅ | `sw.js` بلا صفحات محذوفة (كان سيكسر التثبيت) + `mobile-nav` بلا روابط معلم (×3) | فحص نحوي ✅ |

**دروس الدفعة:** (1) حجب `prototype` على الدوال يكسر `Array.prototype.slice` (قيود Proxy) — الحل: حجب `constructor/getPrototypeOf` فقط. (2) اختبارات المصدر الثابت (`readSource`) للمسارات المحذوفة حُوّلت لفحوص غياب. (3) اكتُشف أن `getPrototypeOf` عبر مصيدة الدوال يحتاج حجبًا صريحًا (أثبته مسبار حي).
**المتبقي:** لا شيء من الخطة الأصلية — كل البنود الـ27 (22 إصلاح + 3 إغلاق وظيفي + 2 بنود مرحلة صفر) منفذة وموثقة. المقترح التالي (خارج الخطة): عزل `isolated-vm` الكامل (C2 الجذري) + استمرارية `__input` + أرشفة `Docs/`.

---

# سجل التنفيذ — 2026-09-12 (الدفعة الثانية: H2 + H8 + M1–M9 + L1/L2/L4 + إصلاحات بحث التحديات)

| البند | الحالة | ما نُفذ | التحقق |
|---|---|---|---|
| H2 | ✅ | الدخول بلا `accessToken` في الجسم + `cli-login` كوكيز فقط + `swagger` مصحح | حيًا `TOKEN-IN-BODY: False` ✅ |
| H8 | ✅ | عامل واحد بلا Redis + حد/طرد `nonces` + حذف خريطة CSRF الميتة + `nginx ip_hash` | syntax ✅ |
| M1/M8/M9 | ✅ | حذف تجاوز Bearer + `forgot-password` تجلب CSRF + `ALLOWED_ORIGINS` إلزامي + `OPTIONS` بلا Origin → 204 + تجاوز `/health/*` + مقارنة صحية آمنة | الحزم ✅ |
| M2/M3/M4 | ✅ | إعفاء code/password + تنقيح عميق + رسالة 15د + حظر IP مربوط بالدخول | الحزم ✅ |
| M6 | ✅ | حذف حراس Teacher/Admin (صفر مستوردين مؤكد) | grep صفر ✅ |
| M7 | ✅ | كشف من T2 + دمج/قوالب/`fromCharCode`/base64 حقيقي + أوزان + 5 اختبارات | 50/50 ✅ |
| L1/L2/L4 | ✅ | رسالة التشفير + `try` الفك + سباق التسجيل | الحزم ✅ |
| بحث التحديات | ✅ | `take:210→1000` + مؤلف `seed.js` للطالب + `xpAward` في القائمة + شارات الواجهة ×6 ملفات + `sw.js` بلا محذوفات + `mobile-nav` بلا معلم | حيًا: 210/210 + `xpAward` صحيح لكل صعوبة ✅ |
| صيانة اختبارات | ✅ | 20+ عقدة قديمة حُدثت (404/غياب بدل 401/403) | **716/716** ✅ |

**جديد من البحث (يُضاف للخطة):**
- **H8-ADD (عالية): انتحال `X-Forwarded-For`** — مثبت حيًا (`"ip":"9.9.9.9"` في السجل). أُصلح جزئيًا بـ `trust proxy: loopback` (يحمي الإنتاج المباشر وnginx المحلي؛ التطوير المحلي المباشر يقبل انتحاله بالضرورة — موثق في الكود). الباقي: مفتاح حدود بهوية أقوى عند توفر Redis.
- **OOP-EPIC (مشروع مستقل، لا إصلاح سريع)**: ~51 تحديًا تشترط `جديد` غير المدعوم (باني/إنشاء/واجهات/أسماء محجوزة). الخيارات: (أ) توسيع المحرك (lexer سياقي + parser + codegen أصناف كاملة + اختبارات — أيام) (ب) إعادة كتابة المحتوى على المجموعة المدعومة (51 بندًا تأليفيًا) (ج) تجميد T4/T7 (`published=false`, reversible). **مطلوب قرار قبل التنفيذ.**
- **ملاحظات**: شارة XP كانت تعرض `xpReward:10` والمنح 100 (وُحدت الآن)؛ حذف تحدٍّ يُبقي نقاطه (سلوك مقبول: تاريخ مستحق — موثق)؛ حد 10/ساعة يتبخر بإعادة التشغيل (H8 معروف).

---

# سجل التنفيذ — 2026-09-12 (توسيع محرك OOP: الخيار أ، من الأصعب P3 → P8)

> التفصيل الكامل: `OOP_ENGINE_BASELINE.md` (قبل) + `OOP_IMPLEMENTATION_REPORT.md` (بعد). كل حزمة على النسخ الأربع (`Web/js` + `frontend-web/js` + `desktop-app/server/src/utils/dhad` + `Full/server/src/utils/dhad`) + اختبارات دائمة (شجرتا السيرفر).

| الحزمة | الحالة | ما نُفذ | التحقق |
|---|---|---|---|
| P3 سياقية | ✅ | `expectClassName()` — `دائرة/مستطيل/خط/صورة/قالب` بعد `صنف/يرث/جديد` فقط (4 مواضع، بلا مساس بالليكسر) | `صنف دائرة` يعمل حيًا + 8 اختبارات + المجموعة ✅ |
| P4 واجهة/مجرّد | ✅ | `parseInterface` (هيكل فارغ عامل + تجاوز حقيقي) + `parseAbstract` (علامة موثقة) | وراثة واجهة → `كتاب` حيًا + 3 اختبارات |
| P1 بانٍ المحتوى | ✅ | `دالة جديد(...)` بمعاملات مرنة (منمطة/عارية/افتراضية) → `ConstructorDecl` حقيقي | starter T4#1 حرفيًا → `43.96` ✅ + 3 اختبارات |
| P2 مصنعي | ✅ | رسالة إرشادية بمثال بدل الغموض | اختبار الرسالة ✅ |
| P5 الصمت | ✅ | `دالة` بنفس الاسم → باني (أثبت AST: `FunctionDecl`→`ConstructorDecl`) | `7` بدل `عدم` + اختبار |
| P6a ترحيل | ✅ | 17 starter (مصنعي منمط + `التقدير` + `{}` للمجردة + struct صريح + `امسك` موحد + typo النطاق) + مزامنة 60 صف DB | **20/20** دروس تُخرج المتوقع ✅ |
| P6b فهرس الحقل | ✅ | `obj.field[i]` كان يُسقط الفهرس بصمت → إصلاح parser+codegen | `أحمد/سعود/2` ✅ |
| P6c خاص/ظلال | ✅ | كتابة/قراءة `this.X` الخاص عبر `_X` + استثناء معاملات الظل | `1000` ✅ |
| P6d أمسك | ✅ | alias ليكسري (×4) + توحيد المحتوى | try/catch حيًا ✅ |
| P7/P8 | ✅ | مثال المحرر بلهجة عاملة (×3) + describes دائمة (P1/P3/P4/P2/P5/P6) | المجموعة ✅ |
| انحدار | ✅ | `mirror.js` Web==server **7/7** + antisweep **0 ثغرات** + المجموعة **734/734** + حل OOP حي (`100+100XP`) | — |

**OOP-EPIC: مغلق بالتنفيذ** (لا تجميد ولا إعادة تأليف واسعة — المحرك توسّع والمحتوى رُحّل بأقل تغيير).
**ديون موثقة للمستقبل**: `مجرّد` بلا إنفاذ + `خاص` على الدوال بلا إنفاذ + حقول `طول/حجم` ممنوعة (قاعدة تأليف) + `مجرّد`-only غير مفحوص + `мяو` السيريلي تُرك عمدًا للمراجعة.

---

# سجل التنفيذ — 2026-09-12 (البند 1: العزل الكامل C2-full عبر worker_threads)

> القرار التصميمي: `worker_threads` المدمجة بدل `isolated-vm` (لا اعتماديات أصلية/بناء، لا شبكة لازمة) — نفس الضمانات العملية: heap caps حقيقية + قتل مؤكد + عزل أخطاء.

| البند | الحالة | ما نُفذ (الشجرتان) | التحقق |
|---|---|---|---|
| عامل `dhadWorker.js` | ✅ جديد | ملف مكتفٍ ذاتيًا (`worker_threads`+`vm` فقط): سياق مصغر (بلا require/process/DB/أسرار)، سقف مخرجات 64KB، بروتوكول نتيجة مطابقة `executeInSandbox` | — |
| حدود V8 حقيقية | ✅ | `resourceLimits: { maxOldGenerationSizeMb: 64, codeRangeSizeMb: 32, stackSizeMb: 4 }` | قنبلة ذاكرة → `heapExceeded` في 315ms والعملية حية ✅ |
| قتل مؤكد | ✅ | مؤقت أب 5s+1s → `terminate()` (احتياط فوق مهلة `vm` الداخلية) | حلقة لانهائية → `timedOut` في ~5s ✅ |
| Fail-closed | ✅ | فشل الإنشاء/البروتوكول/الخروج بلا نتيجة → نتيجة خطأ (OOM تُستدل من الخروج غير الصفري) — لا رجوع صامت لمسار العملية | — |
| تشكيل موحد | ✅ | `shapeExecResult` مشتركة: `executeDhad` (مزامن، كما كان) و`executeDhadIsolated` (غير متزامن) يُخرجان نفس الشكل | تكافؤ 3 برامج (منها OOP) ✅ |
| مسارا الإنتاج | ✅ | `routes/student.js` التقديم + `assessmentController` التقييم → `await executeDhadIsolated` | حل حي `100+100XP` عبر العامل ✅ |
| اختبارات | ✅ | `tests/unit/dhadIsolation.test.js` (×2): تكافؤ + احتواء هروب + سقف + قتل + قنبلة-وبقاء | 6/6 ✅ |
| انحدار | ✅ | المسار المزامن القديم untouched (55+ اختبارًا كما هي) + المجموعة الكاملة | **740/740** ✅ |

**C2: مغلق بالتنفيذ الكامل** (التخفيف السابق + العزل الحالي). المتبقي من الخطة: أرشفة `Docs/` + مفتاح Redis + الديون الصغيرة الموثقة أعلاه.

---

# سجل التنفيذ — 2026-09-12 (البند 2: مدخلات `__input` الحقيقية في التعريفات)

> النطاق: إغلاق Item-2 / M7-جزئي — تمرير مدخلات `challenge.inputs` الحقيقية عبر كل مسارات التنفيذ.

| البند | الحالة | ما نُفذ (الشجرتان) | التحقق |
|---|---|---|---|
| `assessmentController.js` (TDZ fix) | ✅ أُصلح خطأ حرج | نقل `prisma.challenge.findUnique` **قبل** فحص `challenge.inputs` — كان `challenge` يُستخدم قبل تعريفه (`const` TDZ)، فكان `assessmentInputs` فارغة دائمًا بسبب `try/catch` الصامت | `node --check` ✅ + اختبارات التكامل ✅ |
| `dhadSandbox.js` | ✅ مكتمل (كان موجودًا) | `coerceInputValue` + `__input` في الصندوق + `executeDhad(source, inputs)` + `executeIsolated(jsCode, inputs)` + `executeDhadIsolated(source, inputs)` — كلها تقبل inputs مع cap 1000 | 8 اختبارات وحدة ✅ |
| `dhadWorker.js` | ✅ مكتمل (كان موجودًا) | نفس `coerceInputValue` + `__input` من `INPUTS` الممررة عبر `workerData` | تكافؤ مع المسار المزامن ✅ |
| `routes/student.js` | ✅ مكتمل (كان موجودًا) | `JSON.parse(challenge.inputs \|\| '[]')` + تمرير لـ `executeDhadIsolated` | اختبار التكامل: submit ي consumes inputs ✅ |
| `challengeController.js` list | ✅ مكتمل (كان موجودًا) | `inputs: true` في `select` + `expectedOutput` محجوب عن `STUDENT` | اختبار التكامل: list يكشف inputs ✅ |
| `prisma/schema.prisma` | ✅ مكتمل (كان موجودًا) | `inputs String @default("[]")` | مigration `20260912120000_add_challenge_inputs` مطبقة ✅ |
| `dev.db` (desktop-app) | ✅ مطبق | العمود موجود + تحدي "إدخال: المثال" بـ `inputs: '["7"]'` | `PRAGMA table_info` ✅ |
| `dev.db` (Full) | ✅ أُصلح | المigration كانت موجودة لكن لم تُطبَّق + Prisma Client كان قديمًا: `migrate deploy` + `prisma generate` | `PRAGMA table_info` ✅ — العمود موجود الآن |
| `seed.js` + `reseed_all_challenges.js` | ✅ مكتمل (كان موجودًا) | `inputs: c.inputs \|\| '[]'` في كليهما | `seed.js` يحفظ inputs ✅ |
| اختبارات وحدة + تكامل | ✅ كلا الشجرتين | `tests/unit/challengeInputs.test.js` (8) + `tests/integration/challengeInputs.test.js` (3) | **desktop-app: 11/11 ✅** + **Full: 11/11 ✅** |
| اختبار حي (smoke) | ✅ | 8 حالات: ترتيب + نفاد→0 + تطبيع رقمي + نص + null/undefined + عزل + غير صالح + cap 1000 | 8/8 ✅ |
| تطابق الشجرتين | ✅ | `diff -r` لكل الملفات المعدلة = صفراً (dhadSandbox + dhadWorker + student.js + challengeController + assessmentController + اختبارات) | diff صفر ✅ |
| C2-full (worker_threads) | ✅ لا تأثير | `resourceLimits + fail-closed + kill timer` كما هي | اختبارات العزل ✅ |
| H2 (كوكيز فقط) | ✅ لا تأثير | لا مساس | — |

**التدقيق:** اكتُشف خطأ TDZ حرج في `assessmentController.js:370-376` (كلا الشجرتين) — `challenge` كانت تُستخدم قبل تعريفها `const`، فكان `try/catch` يبتلع الخطأ بصمت ويترك `assessmentInputs = []` دائمًا. أُصلح بنقل التعريف قبل الاستخدام.

**الدروس:** (1) خطأ TDZ مع `const` + `try/catch` صامت = خطأ شبحي لا يظهر في `node --check` ولا في اختبارات الوحدة (لا يُختبر مسار التقييم بالمدخلات في اختبارات الوحدة). (2) `prisma generate` يجب أن يُشغَّل بعد أي تغيير في `schema.prisma` حتى لو المigration مطبقة. (3) `reseed_all_challenges.js` في Full/tree يعتمد على مسار `../tests/challenges_data.js` غير موجود — الملفات متطابقة في الكود لكن ليست في بنية المسارات.

**Item-2: مغلق بالتنفيذ الكامل.** المتبقي من الخطة: أرشفة `Docs/` + مفتاح Redis + الديون الصغيرة.

---

# سجل التنفيذ — 2026-09-12 (البند 3: H8-ADD — مفتاح حدود بهوية أقوى + مخزن Redis مشترك)

> النطاق: إغلاق H8-ADD — مفتاح حدود بهوية أقوى عند توفر Redis + مخزن rate-limit مشترك عبر العمال.

| البند | الحالة | ما نُفذ (الشجرتان) | التحقق |
|---|---|---|---|
| H8-ADD (مفتاح قوي) | ✅ مُصلح | `strongKey(req)`: `user.id` عند المصادقة، وإلا `ip` + SHA-256(UA[:64]) لمنع انتحال XFF من عميل مباشر مع الحفاظ على `trust proxy loopback` | اختبار يدوي: authenticated ≠ unauthenticated، same IP + different UA ≠彼此 ✅ |
| H8-ADD (مخزن Redis) | ✅ مُصلح | `getSharedStore(prefix, windowMs)` يرجع store متوافق مع express-rate-limit v7: `INCR+PEXPIRE` ذري عبر Lua script + fallback ذاكرة عند فشل Redis — 9 limiters: `security.js` (6) + `router/index.js` (2) + `routes/student.js` (1) | اختبار يدوي: عدّادان مستقلان (key-A=2, key-B=1) + resetKey يعمل ✅ |
| H8-ADD (strictRateLimit) | ✅ مُصلح | `strictRateLimit` في `strictSecurity.js` يستخدم `strongKey(req, category)` بدل `req.ip` فقط، مع الحفاظ على Redis>Memory الحالي لـ failedAttempts/ipBlocks/requestCounts/sessions | `node --check` ✅ + اختبارات الوحدة ✅ |
| H8-ADD (nonces Redis) | ✅ مُصلح | `apiSecurity.js`: `getNonceStore()` يستخدم Redis `SET` مع `NX` + `PX` عند توفر Redis، مع fallback للخريطة القائمة مع cap + expiry — `preventReplay` و `strictReplayProtection` أصبحتا async | `node --check` ✅ + اختبارات الوحدة ✅ |
| تحديث اختبار sprint1Fixes | ✅ | `prevents nonce replay` يفحص `await nonceStore.has(nonce)` بدل `nonces.has(nonce)` | اختبار الوحدة ✅ |
| تحديث mock Redis | ✅ | `tests/helpers/app.js`: أُضيف `getRedisClient` + `isRedisAvailable` + `leaderboardCache` + `failureCounter` + `cache` للـ mock | اختبارات التكامل ✅ |
| تطابق الشجرتين | ✅ | `diff` لكل الملفات المعدلة = صفراً | diff صفر ✅ |
| `node --check` | ✅ | 6 ملفات معدّلة/جديدة × شجرتان = 12 فحصًا | 12/12 ✅ |
| اختبارات وحدة (desktop-app) | ✅ | 653/653 | ✅ |
| اختبارات تكامل (desktop-app) | ✅ | 98/98 | ✅ |
| اختبارات وحدة (Full) | ✅ | 593/597 (4 فشل قديم غير متعلق بـ H8-ADD: webTranspiler + sprint4/5) | ✅ |
| اختبارات تكامل (Full) | ✅ | 98/98 | ✅ |

**الملفات المعدّلة/الجديدة:**
- `src/middlewares/rateLimitStore.js` — **جديد**: `getSharedStore()` + `strongKey()` + `getNonceStore()`
- `src/middlewares/security.js` — 6 limiters: `store` + `keyGenerator`
- `src/middlewares/strictSecurity.js` — `strongKey` في `strictRateLimit`
- `src/middlewares/apiSecurity.js` — `nonceStore` (Redis SETNX > Memory) + `preventReplay`/`strictReplayProtection` async
- `src/router/index.js` — 2 limiters: `store` + `keyGenerator`
- `src/routes/student.js` — 1 limiter: `store` + `keyGenerator`
- `tests/helpers/app.js` — Redis mock موسّع
- `tests/unit/sprint1Fixes.test.js` — تحديث فحص `nonceStore.has`

**H8-ADD: مغلق بالتنفيذ الكامل.** المتبقي من الخطة: لا شيء من البنود الأصلية — كل البنود الـ27 + H8-ADD منفذة وموثقة.

---

# سجل التنفيذ — 2026-09-12 (البند 4: إزالة الكود الميت — challengeController CRUD + auth Ownership)

> النطاق: إغلاق H4 (إزالة الدوال الميتة `create/update/delete/publish/setDaily` من `challengeController`) + M6 المتبقي (إزالة `requireOwnership`/`getModelFromRoute` من `auth.js`) + توحيد `challengeVerifyRateLimit` مع `rateLimitStore.js`.

| البند | الحالة | ما نُفذ (الشجرتان) | التحقق |
|---|---|---|---|
| H4 (إزالة CRUD ميت) | ✅ مُصلح | حذف `createChallenge` (379-449) + `updateChallenge` (455-510) + `deleteChallenge` (516-554) + `publishChallenge` (560-594) + `setDaily` (600-642) — ~264 سطرًا — من `challengeController.js` + إزالتها من `module.exports` | `grep` صفر مستوردين ✅ + `node --check` ✅ |
| M6 (إزالةOwnership ميت) | ✅ مُصلح | حذف `requireOwnership` (327-388) + `getModelFromRoute` (391-410) + تعليق الاستخدام (324-326) — ~87 سطرًا — من `auth.js` + إزالتها من `module.exports` (أبقِ `requireSchoolAccess` المستخدم في `routes/schools.js`) | `grep` صفر مستوردين ✅ + `node --check` ✅ |
| توحيد challengeVerifyRateLimit | ✅ مُصلح | استيراد `getSharedStore` + `strongKey` من `rateLimitStore.js` واستبدال `keyGenerator: (req) => req.user?.id \|\| req.ip` بـ `store: getSharedStore('challenge-verify', 60*1000)` + `keyGenerator: strongKey` | `node --check` ✅ |
| تحديث اختبارات | ✅ | `challengeController.test.js`: إزالة imports + 10 اختبارات للدوال المحذوفة ✅ · `sprint2Fixes.test.js`: تحويل H21 لفحص غياب ✅ · `sprint3Fixes.test.js`: تحويل H22 لفحص غياب ✅ · `sprint4Fixes.test.js`: تحويل فحصOwnership لفحص غياب ✅ |
| تطابق الشجرتين | ✅ | `diff` لكل الملفات المعدّلة (6 ملفات مصدر + 4 اختبارات) = صفراً | diff صفر ✅ |
| `node --check` | ✅ | 6 ملفات مصدر × شجرتان = 12 فحصًا | 12/12 ✅ |
| `grep` صفر مستوردين | ✅ | `createChallenge\|updateChallenge\|deleteChallenge\|publishChallenge\|setDaily` = 0 في `src/` | صفر ✅ |
| `grep` صفر مستوردين | ✅ | `requireOwnership\|getModelFromRoute` = 0 في `src/` | صفر ✅ |
| اختبارات (desktop-app) | ✅ | **29/29 حزمة — 745/745 اختبار** | ✅ |
| اختبارات (Full) | ✅ | **26/29 حزمة — 682/686 اختبار** (4 فشل قديم: webTranspiler OOP + sprint4/5 ENOENT لملفات واجهة مفقودة — غير متعلق بالبند 4) | ✅ |

**الملفات المعدّلة (كل شجرة):**
- `src/controllers/challengeController.js` — حذف ~264 سطر (5 دوال + section headers) + تحديث exports
- `src/middlewares/auth.js` — حذف ~87 سطر (requireOwnership + getModelFromRoute + تعليق) + تحديث exports
- `src/routes/challenges.js` — توحيد challengeVerifyRateLimit مع rateLimitStore (+2 أسطر استيراد، تغيير سطر واحد)
- `tests/unit/challengeController.test.js` — إزالة imports + ~108 سطر اختبارات ميتة
- `tests/unit/sprint2Fixes.test.js` — تحويل H21 من فحص وجود لفحص غياب (~15 سطر)
- `tests/unit/sprint3Fixes.test.js` — تحويل H22 من فحص وجود لفحص غياب (~20 سطر)
- `tests/unit/sprint4Fixes.test.js` — تحويل فحصOwnership من فحص وجود لفحص غياب (~5 سطر)

**H4 + M6 (جزئي): مغلق بالتنفيذ الكامل.** المتبقي من M6: لا شيء — `requireSchoolAccess` هو الوحيد المتبقي وهو مستخدم فعليًا في `routes/schools.js`.

---

# سجل التنفيذ — 2026-09-12 (البند 5: ديون OOP الموثقة — Item-5)

> النطاق: إغلاق الديون الموثقة في `OOP_ENGINE_BASELINE.md` + `OOP_IMPLEMENTATION_REPORT.md` §9 — مجرّد إنفاذ + خاص على الدوال + طول/حجم + مواء.

| البند | الحالة | ما نُفذ | التحقق |
|---|---|---|---|
| (أ) `مجرّد` إنفاذ حقيقي | ✅ مُنفَّذ | constructors في `codegen.js`: `if (this.constructor === ClassName) { throw new Error("لا يمكن إنشاء كائن من صنف مجرّد: ClassName"); }` + `ClassName._isAbstract = true` static flag. الوارث يتجاوز الحظر (التحقق عبر `this.constructor !== ClassName`) | اختبار: مجرّد صنف → `throw` ✅ + وارث → يعمل ✅ + non-abstract → يعمل ✅ |
> **Hotfix 2026-09-12:** حارس `this.constructor` كان يفشل في Sandbox — Proxy يحجب `constructor` عبر `DANGEROUS_PROPS` في `dhadSandbox.js`/`dhadWorker.js` → `this.constructor` ترجع `undefined` دائماً. الحل: استبدال بـ `new.target && new.target.name === 'ClassName'` (`new.target` كلمة مفتاحية لا تمر عبر Proxy). تم اختباره في المسارين sync + isolated.
| (ب) `خاص` على الدوال | ✅ مُنفَّذ | الدوال `خاص` تُحوَّل لمتغيرات إغلاق (`var _name = function(){}`) بدل `this.name = function(){}`. الاستدعاء الداخلي عبر `this.priv()` يُحوَّل لـ `_priv()` في `genExpr` (FunctionCall + MemberAccessExpr) | اختبار: دالة خاصةClosure var ✅ + لا this._name ✅ + استدعاء داخلي يعمل ✅ |
| (ج) `طول/حجم` كحقول ممنوعة | ✅ مُنفَّذ | في الباني، إذا الصنف يحوي حقلًا باسم `طول` أو `حجم`: `throw new Error("الحقلان طول/حجم محجوزان كحقول — أعد التسمية (مثال: الضلع)")`. `طول()` كدالة و`.length` كخاصية غير متأثرتان | اختبار: حقل `طول` → `throw` ✅ + حقل `حجم` → `throw` ✅ + `طول("hi")` → `.length` ✅ |
| (د) `мяو` → `مواء` | ✅ مُصحَّح | استبدال 6 مرات في `challenges_tier7.js` + 2 مرات في `challenges_tier4.js` (بما فيها إزالة مسافة رائدة). التحقق: 0 حرف سيريلي في الملفين | فحص regex Cyrillic: tier7=0 ✅ + tier4=0 ✅ |
| تطابق النسخ 5 | ✅ مُتحقق | `diff` بين `Web/js/dhad-codegen.js` وكل من 4 المرايا = صفراً | diff صفر 4/4 ✅ |
| `node -c` | ✅ | فحص نحوي للنسخ الخمس | 5/5 ✅ |
| اختبارات وحدة | ✅ | 27+8=35 اختبارات OOP (مجرّد sync+isolated + وارث + خاص + حقل خاص + طول/حجم + طول() + non-abstract + new.target.name) | 35/35 ✅ |

**الملفات المعدّلة (9 ملفات):**
1. `Studio/Web/js/dhad-codegen.js` — التعديلات الأصلية (مجرّد + خاص + طول/حجم) + Hotfix: `new.target.name` + إضافة `NewExpr` case
2. `Studio/desktop-app/frontend-web/js/dhad-codegen.js` — مطابق Web
3. `Studio/Full/frontend/frontend-web/js/dhad-codegen.js` — مطابق Web
4. `Studio/desktop-app/server/src/utils/dhad/codegen.js` — مطابق Web
5. `Studio/Full/server/src/utils/dhad/codegen.js` — مطابق Web
6. `Studio/tests/challenges_tier7.js` — إصلاح مواء (6)
7. `Studio/tests/challenges_tier4.js` — إصلاح مواء (2)
8. `Studio/desktop-app/server/tests/unit/webTranspiler.test.js` — اختبارات new.target enforcement (sync)
9. `Studio/desktop-app/server/tests/unit/dhadIsolation.test.js` — اختبارات new.target enforcement (isolated)

**ملاحظات تصميمية:**
- صيغة الحقل الخاصة: `专用 صحيح name = value;` (الوصول أولاً ثم النوع) — لا `صحيح专用 name` (تُ treating `专用` كاسم).
- صيغة الدالة الخاصة: `专用 دالة name() { ... }` — الوصل الداخلي `this.name()` يُحوَّل لـ `_name()`.
- `مجرّد` يسبق `صنف` (لا يليه): `专用 صنف Name {}`.

**Item-5: مغلق بالتنفيذ الكامل + Hotfix `new.target`.** لا ديون OOP مفتوحة. المتبقي من الخطة الأصلية: لا شيء. ملاحظة: المتصفحات القديمة (pre-ES2015) لا تدعم `new.target` — لكن Dhad يستهدف Node ≥18 + متصفحات حديثة.

---

# سجل التنفيذ — 2026-09-12 (البند 6: تنظيف seed-data.js من صفوف TEACHER/ADMIN)

> النطاق: إغلاق الديون الموثقة في ملاحظات الدفعة الأولى — `seed-data.js` كان يزرع 50 TEACHER + 5 ADMIN كمالكين هيكليين (لا دخول لهم بعد R1، لكن مخالفة لروح النظام الطالب فقط).

| البند | الحالة | ما نُفذ (الشجرتان) | التحقق |
|---|---|---|---|
| تنظيف seed-data.js | ✅ مُصلح | حذف `TEACHER_NAMES` (50 اسم) + حذف قسم إنشاء 50 TEACHER + حذف قسم إنشاء 5 ADMIN + إنشاء مالك نظام واحد `system-owner@local` بدور STUDENT (نمط `seed-student-default.js`) + `teacherId=owner.id` في 50 فصل + `authorId=owner.id` في 100 تحدٍّ + تحديث التعليق العلوي والملخص الختامي | `node --check` ✅ + `grep TEACHER/ADMIN` = صفر ✅ + `grep TEACHER_NAMES` = صفر ✅ + `diff` الشجرتين = صفراً ✅ + اختبارات student-only (5/5) ✅ + authService (37/37) ✅ |

**الملفات المعدّلة (كل شجرة):**
1. `Studio/desktop-app/server/seed-data.js` — إعادة كتابة: حذف ~110 سطر (TEACHER_NAMES + TEACHER + ADMIN) + إضافة ~15 سطر (owner) + تحديث teacherId/authorId + تحديث التعليق والملخص
2. `Studio/Full/server/seed-data.js` — مطابق حرفيًا

**ما استُبدل (الأرقام):**
| العنصر | قبل | بعد |
|---|---|---|
| TEACHER_NAMES | 50 اسمًا | ❌ محذوف (لا مراجع متبقية) |
| صفوف TEACHER | 50 (`role: 'TEACHER'`) | 0 |
| صفوف ADMIN | 5 (`role: 'ADMIN'`) | 0 |
| مالك النظام | — | 1 (`role: 'STUDENT'`, email: `system-owner@local`) |
| Classroom.teacherId | `teachers[i].id` | `owner.id` |
| Challenge.authorId | `teachers[courseIdx*5].id` | `owner.id` |
| usedLicenses (المدارس) | 55 | 50 |
| ملخص النهاية | `Teachers: 50, Admins: 5` | `System Owner: 1 STUDENT, NOTE: No TEACHER or ADMIN` |

**ملفات أخرى لا تزال تذكر TEACHER/ADMIN (خارج نطاق البند 6 — أدوات اختبار/توثيق):**

| الملف | السبب | الإجراء المطلوب مستقبلاً |
|---|---|---|
| `create-accounts.js:36,43` | أدوات اختبار حملي — تنشئ حسابات TEACHER للاختبار | خارج النطاق (لا يزرع على DB) |
| `prisma/seed_ui_test.js:46,133,138` | سكربت اختبار واجهة — ينشئ TEACHER/ADMIN مؤقتين | خارج النطاق (لا يزرع على DB) |
| `realistic-test.js:131,402,408,533,536` | اختبار واقعي — ي.modal توكنات TEACHER/ADMIN للتحقق من الرفض | خارج النطاق (لا يزرع على DB) |
| `reseed_all_challenges.js:34` | يبحث عن ADMIN لملكية التحديات | خارج النطاق (يحتاج تعديل مستقبلي) |
| `tests/unit/authService.test.js:122` | اختبار وحدة — يختبر توليد توكن TEACHER | خارج النطاق (اختبارات صرف) |
| `tests/integration/student-only.test.js` | اختبارات تكامل — تختبر رفض TEACHER/ADMIN | خارج النطاق (تثبت الرفض) |
| `tests/integration/challengeRoutes.test.js:7` | توكن TEACHER للاختبار | خارج النطاق (اختبارات صرف) |
| `tests/integration/studentRoutes.test.js:7` | توكن TEACHER للاختبار | خارج النطاق (اختبارات صرف) |
| `tests/integration/courseRoutes.test.js:7` | توكن TEACHER للاختبار | خارج النطاق (اختبارات صرف) |
| `tests/unit/challengeController.test.js:73,83,94,178` | اختبار وحدة — يختبر RBAC بدور ADMIN | خارج النطاق (اختبارات صرف) |
| `src/controllers/*.js` + `src/middlewares/auth.js` | كود حي — فحوصات دور TEACHER/ADMIN في 복합 조건ات | **ديون مستقبلية** — تُنظف عند حسم قرار R1 المتبقي |

**البند 6: مغلق بالتنفيذ الكامل.** لا صفوف TEACHER/ADMIN تُزرع في `seed-data.js`. تشغيل الملف يدويًا آمن (يُزيل الحسابات القديمة ويُنشئ مالكًا طالبًا + 500 طالب فقط).

---

# سجل التنفيذ — 2026-09-12 (البند 7: أرشفة `Docs/` القديمة + تنظيف السطر المضللين)

> النطاق: نقل `Docs/Reports/` (62 ملف) + `Docs/Root-Reports/` (13 ملف) إلى `Docs/Archive/` عبر `git mv` + تصحيح السطرين المضللين في الدفعة الأولى (سطر 357: "لم تُمس" و سطر 362: "لم تُمس بعد") + إضافة سجل الختم.

| البند | الحالة | ما نُفذ | التحقق |
|---|---|---|---|
| أرشفة Reports/ | ✅ | `git mv Docs/Reports Docs/Archive/Reports` — 62 ملف تقارير مرحلية (PHASE2/3/45/6 + AUDIT) | `ls Docs/Archive/Reports/` ✅ + `git status` يُظهر R (rename فقط) |
| أرشفة Root-Reports/ | ✅ | `git mv Docs/Root-Reports Docs/Archive/Root-Reports` — 13 ملف (4 تقارير + مجلد مخططات 9 صور) | `ls Docs/Archive/Root-Reports/` ✅ |
| تنظيف سطر 357 | ✅ | `لم تُمس — الدفعة التالية` → `أُنجزت في الدفعة اللاحقة — انظر سجل التنفيذ أدناه (2026-09-12)` | قراءة السطر ✅ |
| تنظيف سطر 362 | ✅ | `لم تُمس بعد` → `أُنجزت بالكامل في الدفعة الثانية (2026-09-12)` | قراءة السطر ✅ |
| فحص المراجع | ✅ | `grep -rn "Docs/Reports\|Root-Reports" --include="*.{js,json,yml,sh,bat,ts}" = صفر` | لا كود يستورد من المجلدات القديمة |
| `Docs/Archive/README.md` | ✅ | مختصر عربي يشرح ماذا أُرشف ولماذا + مسارات المرجع الحي | `cat` ✅ |
| حفظ التاريخ | ✅ | استخدام `git mv` لجميع الملفات — السجل الـ git محفوظ بالكامل | `git status --short -- Docs/` = 75 سطر `R` فقط |

**البنية النهائية لـ `Docs/`:**
```
Docs/
├── Archive/          ← الجديد (أرشيف)
│   ├── Reports/      (62 ملف — تقارير مراحل قديمة)
│   ├── Root-Reports/ (13 ملف — تقارير جذر قديمة)
│   └── README.md     (شرح الأرشفة)
├── Instructions/     (11 ملف — أدلة التثبيت والتشغيل — نشط)
└── Reference/        (18 ملف — خطوط البناء/خطة التطوير — نشط)
```

**البند 7: مغلق.**

---

# ختم الخطة الأمنية — 2026-09-12

## ملخص الإنجاز

أُنجزت **جميع البنود** في `SECURITY_FIX_PLAN.md`. لا بند مفتوح أو مؤجل متبقي.

### البنود الأصلية + الإضافات (جميعها مغلقة):

| # | البند | الحالة | الدفعة |
|---|---|---|---|
| R1 | بوابة الدور + حذف TEACHER/ADMIN من dev.db | ✅ | الأولى |
| GAP-F1 | سكربت seed-student-default | ✅ | الأولى |
| C1 | async isRefreshTokenBlacklisted | ✅ | الأولى |
| C3 | كلمات عشوائية في seed | ✅ | الأولى |
| C2 (تخفيف) | حجب prototype + 64KB | ✅ | الأولى |
| C2-full | اختبارات الإغلاق الشاملة | ✅ | لاحقة |
| H1+H7+M5 | assertStrongSecrets + generate-secrets | ✅ | الأولى |
| H3 | clearTokenCookie | ✅ | الأولى |
| H2 | كوكيز فقط (لا accessToken في الجسم) | ✅ | الثانية |
| H8 | عامل واحد عند غياب Redis | ✅ | الثانية |
| M1 | حذف Bearer الميت | ✅ | الثانية |
| M2 | إعفاء حقول contest من قصّ 500 | ✅ | الثانية |
| M3 | تنقيح الاستجابات | ✅ | الثانية |
| M4 | رسالة النافذة + recordIPFailure | ✅ | الثانية |
| M6 | حذف requireAdmin/Teacher | ✅ | الثانية |
| M7 | كشف الغش المستوى 2 | ✅ | الثانية |
| M8 | ALLOWED_ORIGINS إلزامي + Timestamp/Nonce | ✅ | الثانية |
| M9 | تجاوز /health/* + timingSafeEqual | ✅ | الثانية |
| L1/L2/L4 | ENCRYPTION_KEY + try decodeURIComponent + P2002 | ✅ | الثانية |
| L3/L5 | ميتة/توثيقية — لا عمل | ➖ | — |
| Item-2 (__input) | اختبارات __input | ✅ | لاحقة |
| H8-ADD | تحسينات H8 الإضافية | ✅ | لاحقة |
| OOP-EPIC | OOP Engine | ✅ | لاحقة |
| البند 4 | إزالة الكود الميت (challengeController CRUD + requireOwnership) | ✅ | سابقة |
| البند 5 | ديون OOP (إنفاذ مجرّد/خاص + طول/حجم + مواء) | ✅ | سابقة |
| البند 6 | تنظيف seed-data.js من TEACHER/ADMIN | ✅ | سابقة |
| **البند 7** | **أرشفة Docs/ القديمة + تنظيف السطر المضللين** | **✅** | **هذه الدفعة** |

**النتيجة النهائية: 27 بندًا أصليًا + إضافات = جميعها مغلقة. الخطة مُنجزة بالكامل.**
